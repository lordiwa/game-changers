---
phase: quick-260507-i16
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/package.json
  - packages/shared/tsconfig.build.json
  - package.json
  - pnpm-lock.yaml
  - firebase.json
autonomous: true
requirements:
  - QUICK-260507-i16
must_haves:
  truths:
    - "Cloud Build no longer fails with EUNSUPPORTEDPROTOCOL workspace:* during firebase deploy"
    - "@gamechangers/shared has a real lib/index.js artifact emitted from tsc, not a TS-source main field"
    - "Root package.json depends on firebase-tools-with-isolate (not upstream firebase-tools) and isolate-package"
    - "All 8 codebases in firebase.json have isolate: true and a 3-step predeploy build chain"
    - "firebase deploy --only functions:auth completes successfully with all 5 auth functions deployed to southamerica-east1"
  artifacts:
    - path: "packages/shared/package.json"
      provides: "Updated main/types pointing at lib/, build script, real exports map"
      contains: "lib/index.js"
    - path: "packages/shared/tsconfig.build.json"
      provides: "Build-only tsconfig that emits to lib/"
      contains: "outDir"
    - path: "packages/shared/lib/index.js"
      provides: "Compiled JS entry point for shared package"
    - path: "package.json"
      provides: "firebase-tools-with-isolate + isolate-package devDeps (no firebase-tools)"
      contains: "firebase-tools-with-isolate"
    - path: "firebase.json"
      provides: "isolate: true and predeploy arrays for all 8 codebases"
      contains: "\"isolate\": true"
  key_links:
    - from: "firebase.json codebases[].predeploy"
      to: "packages/shared/lib/ + functions/<codebase>/lib/"
      via: "pnpm --filter ... build"
      pattern: "pnpm --filter @gamechangers/shared build"
    - from: "firebase deploy"
      to: "Cloud Build npm install (workspace:* rewritten to file: refs)"
      via: "isolate-package via firebase-tools-with-isolate fork"
      pattern: "\"isolate\": true"
    - from: "@gamechangers/functions-auth"
      to: "@gamechangers/shared/lib/index.js"
      via: "isolated node_modules with materialised internal deps"
      pattern: "main.*lib/index.js"
---

<objective>
Resolve `EUNSUPPORTEDPROTOCOL workspace:*` blocking `firebase deploy` by adopting `isolate-package` via the `firebase-tools-with-isolate` fork. Simultaneously fix the `@gamechangers/shared` TS-source `main` field that would prevent Cloud Run from executing the package even after isolation succeeds.

Purpose: Unblock backend Cloud Functions deployment to `gamechangers-prod`. Without this, no function in any of the 8 codebases can deploy because Cloud Build's `npm install` cannot resolve `workspace:*` references.

Output: Updated `firebase.json` (8 codebases × isolate flag), updated `packages/shared` (real build output), swapped root devDeps, validated by deploying the `auth` codebase end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260507-i16-resolve-eunsupportedprotocol-workspace-e/260507-i16-RESEARCH.md
@./CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase reads during planning. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

Current `packages/shared/package.json`:
```json
{
  "name": "@gamechangers/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "node ../../node_modules/vitest/vitest.mjs run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": { "zod": "4.3.6" },
  "devDependencies": { "typescript": "5.9.3", "vitest": "4.1.5" },
  "exports": {
    ".": "./src/index.ts",
    "./schemas": "./src/schemas/index.ts",
    "./types": "./src/types/index.ts",
    "./xp": "./src/xp.ts"
  }
}
```

Current `packages/shared/src/` layout (verified):
- `index.ts` (re-exports schemas, types, xp)
- `schemas/index.ts`
- `types/index.ts`
- `xp.ts`
- `__tests__/` (excluded from build via tsconfig.base.json `exclude: ["**/*.test.ts"]`)

Current `packages/shared/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "outDir": "dist", "rootDir": "src" },
  "include": ["src/**/*.ts"]
}
```

Note: existing `tsconfig.json` uses `outDir: "dist"`. New `tsconfig.build.json` MUST use `outDir: "lib"` (matches `main: lib/index.js` and exports map). Do NOT touch the existing `tsconfig.json` (used by `typecheck`).

`tsconfig.base.json` already sets: `module: NodeNext`, `moduleResolution: NodeNext`, `declaration: true`, `sourceMap: true`. So `tsconfig.build.json` only needs to override `outDir`, `rootDir`, and ensure `noEmit` is false (default).

Existing `firebase.json` codebase entries (all 8 follow this exact shape):
```json
{
  "codebase": "<name>",
  "source": "functions/<name>",
  "runtime": "nodejs22",
  "region": "southamerica-east1"
}
```
Codebases (in order, from firebase.json): auth, consent, events, challenges, wearables, gamification, b2b, trustsafety.

Each codebase's package.json `name` field follows the pattern `@gamechangers/functions-<codebase>` (verified: `functions/auth/package.json` has `name: "@gamechangers/functions-auth"`).

Root `package.json` devDependencies currently include `"firebase-tools": "^15.15.0"` — this MUST be removed and replaced with `firebase-tools-with-isolate@^15.16.0` and `isolate-package@^1.32.1`.

`functions/shared/package.json` (already correct, DO NOT MODIFY): `main: "lib/index.js"`, `types: "lib/index.d.ts"`, full `exports` map for `./hmac`, `./kms`, `./botAuth`, `./sentry`, `./types`, `./ConsentEnforcement`, `./.`. Build script `tsc` exists. This was finalised in commit 2b66e7c (quick task 260507-hag).
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lib/ build for @gamechangers/shared</name>
  <files>packages/shared/package.json, packages/shared/tsconfig.build.json</files>
  <action>
Make `@gamechangers/shared` emit a real `lib/` JS artifact so Cloud Run can execute it after isolation. Two file changes:

(a) Create `packages/shared/tsconfig.build.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "lib",
    "rootDir": "src",
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  },
  "include": ["src/**/*.ts"],
  "exclude": ["src/**/*.test.ts", "src/__tests__/**"]
}
```

(b) Update `packages/shared/package.json` — change `main`/`types` to point at `lib/`, add `build` script, replace `exports` map with one mirroring all subpaths against compiled JS:

```json
{
  "name": "@gamechangers/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "node ../../node_modules/vitest/vitest.mjs run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "4.3.6"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "4.1.5"
  },
  "exports": {
    ".":         { "types": "./lib/index.d.ts",         "default": "./lib/index.js" },
    "./schemas": { "types": "./lib/schemas/index.d.ts", "default": "./lib/schemas/index.js" },
    "./types":   { "types": "./lib/types/index.d.ts",   "default": "./lib/types/index.js" },
    "./xp":      { "types": "./lib/xp.d.ts",            "default": "./lib/xp.js" }
  }
}
```

DO NOT touch the existing `tsconfig.json` (used by `typecheck` script — must remain pointing at `dist` outDir or it will not interfere).

Why: per RESEARCH.md §P3, even after isolation succeeds, Cloud Run cannot execute `.ts` source — `main` must point at compiled JS. This is mandatory for any deploy strategy, not isolate-specific.
  </action>
  <verify>
    <automated>cd packages/shared && pnpm build && test -f lib/index.js && test -f lib/schemas/index.js && test -f lib/types/index.js && test -f lib/xp.js && test -f lib/index.d.ts</automated>
  </verify>
  <done>`packages/shared/lib/index.js`, `lib/schemas/index.js`, `lib/types/index.js`, `lib/xp.js` and matching `.d.ts` files exist. `pnpm --filter @gamechangers/shared build` exits 0. Existing `pnpm typecheck` from repo root still passes (consumers — PWA, functions — resolve the package via the new `exports` map).</done>
</task>

<task type="auto">
  <name>Task 2: Swap firebase-tools for firebase-tools-with-isolate fork</name>
  <files>package.json, pnpm-lock.yaml</files>
  <action>
Replace upstream `firebase-tools` with the community fork that integrates `isolate-package` into the deploy pipeline. CRITICAL ordering per the fork README — uninstall first, then install fork; both packages provide the `firebase` binary and PATH precedence is ambiguous if both are installed.

From repo root, run:
```bash
pnpm remove -D firebase-tools
pnpm add -D firebase-tools-with-isolate@^15.16.0 isolate-package@^1.32.1
```

The fork tracks upstream daily and exposes the same `firebase` binary. Verify the resulting `package.json` devDependencies block contains `firebase-tools-with-isolate` and `isolate-package` and does NOT contain `firebase-tools`.

Document the supply-chain decision in the eventual SUMMARY (this is a community fork, not first-party Firebase tooling) — note this in commit message.
  </action>
  <verify>
    <automated>firebase --version && grep -q firebase-tools-with-isolate package.json && grep -q isolate-package package.json && ! grep -E '"firebase-tools"\s*:' package.json</automated>
  </verify>
  <done>`firebase --version` prints a 15.x version. `package.json` devDependencies contains `firebase-tools-with-isolate@^15.16.0` and `isolate-package@^1.32.1`, and no longer contains a top-level `firebase-tools` key. `pnpm-lock.yaml` reflects the swap (no `firebase-tools` package entry from upstream registry, but a `firebase-tools-with-isolate` entry).</done>
</task>

<task type="auto">
  <name>Task 3: Add isolate + predeploy to all 8 firebase.json codebases</name>
  <files>firebase.json</files>
  <action>
Update every entry under `functions[]` in `firebase.json` to add `"isolate": true` and a 3-element `"predeploy"` array. The 8 codebases are: auth, consent, events, challenges, wearables, gamification, b2b, trustsafety.

For each codebase, the predeploy array must build (a) the workspace lib package, (b) the shared functions package, (c) the codebase itself — in that order. Example for `auth`:

```json
{
  "codebase": "auth",
  "source": "functions/auth",
  "runtime": "nodejs22",
  "region": "southamerica-east1",
  "isolate": true,
  "predeploy": [
    "pnpm --filter @gamechangers/shared build",
    "pnpm --filter @gamechangers/functions-shared build",
    "pnpm --filter @gamechangers/functions-auth build"
  ]
}
```

The third predeploy entry's `--filter` package name varies per codebase: `@gamechangers/functions-<codebase>` where `<codebase>` is the codebase name verbatim (auth/consent/events/challenges/wearables/gamification/b2b/trustsafety). Verified: `functions/auth/package.json` declares `name: "@gamechangers/functions-auth"`, and the rest follow the same pattern.

Per RESEARCH.md §P2, predeploy hooks run only for matched codebases under `--only functions:<name>`, so each codebase's predeploy MUST include all its workspace dep builds explicitly — DO NOT factor them out to a global hook.

Per RESEARCH.md §P1, the `"isolate"` field is a fork-specific extension; Firebase CLI may print a non-fatal schema warning. Accept the warning; do NOT remove `isolate: true` to silence it.

Leave `firestore`, `storage`, `hosting`, `emulators` blocks untouched.
  </action>
  <verify>
    <automated>node -e "const c=require('./firebase.json'); const fns=c.functions; if(fns.length!==8)throw new Error('expected 8 codebases'); for(const f of fns){ if(f.isolate!==true) throw new Error('missing isolate on '+f.codebase); if(!Array.isArray(f.predeploy)||f.predeploy.length!==3) throw new Error('bad predeploy on '+f.codebase); if(!f.predeploy[2].includes('@gamechangers/functions-'+f.codebase)) throw new Error('predeploy[2] mismatch on '+f.codebase);} console.log('OK')"</automated>
  </verify>
  <done>All 8 entries in `firebase.json` `functions[]` have `isolate: true` and a 3-element `predeploy` array whose third element matches `pnpm --filter @gamechangers/functions-<codebase> build`. `node -e` validation script (above) prints "OK".</done>
</task>

<task type="auto">
  <name>Task 4: Test deploy with auth codebase</name>
  <files>(no source files — pure deploy validation)</files>
  <action>
Execute a real deploy of the `auth` codebase to validate the fix end-to-end. From repo root:

```bash
firebase deploy --only functions:auth --project gamechangers-prod
```

Capture full stdout + stderr. SUCCESS criteria:
- The previously-failing `EUNSUPPORTEDPROTOCOL workspace:*` error is GONE.
- All 5 auth functions report `creating Node.js 22 (2nd Gen) function ... successful` (or `updating ... successful` if they already exist): `verifyAge`, `anonUpgrade`, `discordExchange`, `unlinkDiscord`, `botGenerateLinkToken`.
- Final summary line: `Deploy complete!` (or equivalent green status).

If a NEW error surfaces that is unrelated to workspace-protocol (e.g. a pre-existing TypeScript error in `functions/auth/src`, a missing secret, a Cloud Build IAM issue), STOP and document the new error verbatim in the task notes. DO NOT attempt to fix unrelated errors in this task — the success metric for THIS quick task is "workspace-protocol error gone", not "all functions in production".

Schema warning re `"isolate"` field is expected and non-fatal per RESEARCH.md §P1 — note its presence in the task report but do not act on it.
  </action>
  <verify>
    <automated>firebase functions:list --project gamechangers-prod | grep -E "verifyAge|anonUpgrade|discordExchange|unlinkDiscord|botGenerateLinkToken" | wc -l | grep -qE '^\s*5\s*$'</automated>
  </verify>
  <done>`firebase functions:list --project gamechangers-prod` shows all 5 auth functions (`verifyAge`, `anonUpgrade`, `discordExchange`, `unlinkDiscord`, `botGenerateLinkToken`) deployed in `southamerica-east1`. The deploy log no longer contains `EUNSUPPORTEDPROTOCOL` or `workspace:*` errors. Subpath imports from `@gamechangers/functions-shared/botAuth` etc. resolved at cold-start (no MODULE_NOT_FOUND in the deploy log).</done>
</task>

<task type="auto">
  <name>Task 5: Deploy remaining 7 codebases (CONDITIONAL on Task 4 success)</name>
  <files>(no source files — full deploy)</files>
  <action>
ONLY EXECUTE IF Task 4 succeeded (all 5 auth functions deployed, no workspace-protocol error). If Task 4 failed for a reason unrelated to workspace-protocol, document and skip this task.

Deploy the remaining 7 codebases. Two acceptable strategies — pick whichever seems gentler on Cloud Build quota:

**Option A — all-at-once:**
```bash
firebase deploy --only functions --project gamechangers-prod
```

**Option B — pairwise (gentler on parallel Cloud Build quota):**
```bash
firebase deploy --only functions:consent,functions:events --project gamechangers-prod
firebase deploy --only functions:challenges,functions:wearables --project gamechangers-prod
firebase deploy --only functions:gamification,functions:b2b --project gamechangers-prod
firebase deploy --only functions:trustsafety --project gamechangers-prod
```

For each codebase, success = "creating/updating function ... successful" with no `EUNSUPPORTEDPROTOCOL` and no `workspace:*` error.

If a codebase fails with a NEW error UNRELATED to workspace-protocol (pre-existing TS errors, missing secrets, etc.) — DOCUMENT and SKIP. Per the Camino B success metric agreed in research, this quick task is complete when the workspace-protocol error is gone, not when every codebase is operational. Other codebases have their own pre-existing issues per the 260507-hag investigation.

Record per-codebase deploy outcome in the SUMMARY's "Deploy Outcomes" table.
  </action>
  <verify>
    <automated>firebase functions:list --project gamechangers-prod | grep -cE 'southamerica-east1' | grep -qvE '^\s*0\s*$'</automated>
  </verify>
  <done>Either: (a) all 8 codebases successfully deployed with zero `EUNSUPPORTEDPROTOCOL`/`workspace:*` errors; OR (b) the codebases that failed did so for documented reasons unrelated to workspace-protocol (e.g. pre-existing TS build errors in their own source). The SUMMARY enumerates per-codebase outcomes.</done>
</task>

</tasks>

<verification>
End-to-end success of this quick task = the `EUNSUPPORTEDPROTOCOL workspace:*` error class is permanently eliminated from `firebase deploy`. Concretely:

1. `firebase deploy --only functions:auth --project gamechangers-prod` runs without ever printing `EUNSUPPORTEDPROTOCOL` or `workspace:*` parse errors. (Task 4 — primary success gate.)
2. `packages/shared/lib/index.js` is the resolved `main` for the package (real JS, not TS source). Cloud Run cold-starts can `require` it. (Task 1.)
3. `firebase --version` continues to work (fork drop-in preserves CLI surface). (Task 2.)
4. `firebase.json` codebase entries each carry `isolate: true` + 3-element predeploy. The fork-specific schema warning is expected and non-fatal. (Task 3.)
5. Local `firebase emulators:start` continues to work unchanged — isolation only runs during deploy, never during emulator start (RESEARCH.md §P8).

Cross-cuts:
- DO NOT modify `functions/shared/package.json` — already correct since 260507-hag.
- DO NOT bundle (option B rejected), DO NOT use `pnpm deploy --filter` (option C rejected), DO NOT `sed`-rewrite `workspace:*` (option D rejected).
- DO NOT touch any function's source code in `functions/<name>/src/`.
- DO NOT touch `apps/pwa`, `apps/discord-bot`, `tests/*`.
</verification>

<success_criteria>
- [ ] `packages/shared/lib/index.js` exists and is loadable as ESM (real compiled JS).
- [ ] `packages/shared/package.json` has `main: "lib/index.js"`, `types: "lib/index.d.ts"`, `build` script, and an `exports` map mirroring all 4 subpaths against `lib/`.
- [ ] `packages/shared/tsconfig.build.json` exists, extends the base config, emits to `lib/`, excludes tests.
- [ ] Root `package.json` devDeps: contains `firebase-tools-with-isolate@^15.16.0` and `isolate-package@^1.32.1`; does NOT contain `firebase-tools`.
- [ ] `firebase --version` works.
- [ ] `firebase.json` has all 8 functions entries with `isolate: true` and a 3-element `predeploy` array; the 3rd predeploy entry per codebase references the correct `@gamechangers/functions-<codebase>` filter.
- [ ] `firebase deploy --only functions:auth --project gamechangers-prod` completes with all 5 auth functions deployed in `southamerica-east1` and zero `EUNSUPPORTEDPROTOCOL`/`workspace:*` errors in the log.
- [ ] Remaining 7 codebases either deploy or fail for documented unrelated reasons; per-codebase outcomes recorded in SUMMARY.
- [ ] SUMMARY documents the supply-chain decision to use the `firebase-tools-with-isolate` community fork.
</success_criteria>

<output>
After completion, create `.planning/quick/260507-i16-resolve-eunsupportedprotocol-workspace-e/260507-i16-SUMMARY.md` with:
- Per-codebase deploy outcome table (8 rows, status + notes)
- Note on supply-chain decision (firebase-tools-with-isolate fork)
- Any first-deploy verification of assumptions A1 (schema warning non-fatal) and A2 (subpath exports resolve at cold-start)
- Followups list (any unrelated errors discovered in non-auth codebases that need their own quick tasks)
</output>
