---
phase: 02-platform-mvp
plan: 14
type: execute
wave: 8
depends_on: []
gap_closure: true
files_modified:
  - scripts/backfill-counters.ts
  - scripts/package.json
  - scripts/__tests__/backfill-counters.test.ts
  - docs/operations/backfill-counters-runbook.md
autonomous: true
requirements:
  - PROF-05
gap_closure_targets:
  - G3

must_haves:
  truths:
    - "`scripts/backfill-counters.ts` exists and is invocable as `pnpm --filter @gamechangers/scripts exec tsx backfill-counters.ts --project=<id> [--dry-run] [--uid=<uid>]`"
    - "Script iterates `auditLog` collection (or per-user `users/{uid}/auditLog`, whichever the WR-06 fix writes to — verified by reading the source) and aggregates per-uid: count of `event_attended` actions → `eventAttendedTotal`; count of `content_completed` → `contentCompletedTotal`"
    - "Script writes aggregates to `users/{uid}/profile/main.eventAttendedTotal` and `.contentCompletedTotal` using `FieldValue.set` with `merge: true` — does NOT overwrite other profile fields"
    - "Script is IDEMPOTENT: re-running on the same data produces the same result (proven by re-running and asserting no diff in profile docs)"
    - "Script supports `--dry-run` mode: prints the diff without writing"
    - "Script has unit tests against the Firebase emulator: seed N audit events for M users → run script → assert profile docs hold expected counts"
    - "Operator runbook documents: prerequisites (service-account key with Datastore User), command, expected runtime per 1K users, rollback (no rollback — counters are deterministic, but a `--dry-run` confirms the diff)"
  artifacts:
    - path: "scripts/backfill-counters.ts"
      provides: "One-shot Node/tsx script computing eventAttendedTotal + contentCompletedTotal per user"
    - path: "scripts/__tests__/backfill-counters.test.ts"
      provides: "Vitest test against Firebase emulator validating aggregation correctness + idempotency"
    - path: "docs/operations/backfill-counters-runbook.md"
      provides: "Runbook for executing the script in production"
  key_links:
    - from: "auditLog entries with action in {event_attended, content_completed}"
      to: "users/{uid}/profile/main counters"
      via: "Aggregation by uid + action type"
      pattern: "FieldValue.increment OR direct set"
---

<objective>
Author a one-shot, idempotent backfill script that initializes `eventAttendedTotal` + `contentCompletedTotal` on existing `/users/{uid}/profile/main` from historical `auditLog` data. Without this, existing users (when first onboarded) will have zero counters even if they have prior `event_attended` or `content_completed` audit entries from before WR-06 introduced the denormalized counters. The script is testable against the Firebase emulator (deterministic) and runnable on-demand against the live project.

Purpose: Closes G3 (MEDIUM) from `02-HUMAN-UAT.md`. Independent of any other plan — pure code + emulator test work, no infrastructure dependency. WR-06 fix introduced live counters via `FieldValue.increment(1)` on writes after deploy; this plan adds the historical lookback.

Output: `scripts/backfill-counters.ts` exists with passing emulator-backed tests; runbook documents production execution; counters are reproducible (idempotent re-run produces no diff).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-platform-mvp/02-HUMAN-UAT.md
@.planning/phases/02-platform-mvp/02-05-PLAN.md
@.planning/phases/02-platform-mvp/02-05-SUMMARY.md
@CLAUDE.md
@scripts/package.json
@functions/gamification/src/xpAward.ts

<interfaces>
```typescript
// auditLog entry shape (from WR-06 fix in xpAward.ts + checkIn.ts):
interface AuditLogEntry {
  uid: string;
  action: 'event_attended' | 'content_completed' | 'consent_granted' | 'consent_revoked' | ... ;
  timestamp: Timestamp;
  metadata?: Record<string, unknown>;
}

// Profile target document shape (relevant fields only):
interface UserProfile {
  // ... other fields
  eventAttendedTotal: number;
  contentCompletedTotal: number;
}

// Script CLI:
//   tsx scripts/backfill-counters.ts --project=gamechangers-prod [--dry-run] [--uid=<single-uid>] [--batch-size=500]
//
// Exit codes:
//   0 success
//   1 generic error
//   2 missing credentials
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write emulator-backed test for backfill script (RED)</name>
  <files>
    scripts/__tests__/backfill-counters.test.ts,
    scripts/package.json
  </files>
  <read_first>
    scripts/package.json (existing — confirm tsx, vitest, firebase-admin, @firebase/rules-unit-testing deps; add if missing),
    functions/events/src/checkIn.ts (where 'event_attended' audit entries are written — confirm exact action string),
    functions/content/src/contentCompleted.ts (where 'content_completed' is written — confirm exact action string)
  </read_first>
  <behavior>
    - Test seeds N=10 users with varying audit-log entries:
      - User A: 5 `event_attended` + 3 `content_completed`
      - User B: 0 `event_attended` + 7 `content_completed`
      - User C: 100 `event_attended` (volume case) + 0 `content_completed`
      - User D: 0 of either (empty case → counters should be 0)
      - Users E–J: random small counts
    - Each user has a pre-existing `users/{uid}/profile/main` doc with `displayName: '...'` and OTHER fields (to assert merge doesn't overwrite).
    - Run script in `--dry-run` mode → assert NO Firestore writes happen; assert dry-run output matches expected counts.
    - Run script for real → assert profile docs hold:
      - `users/A/profile/main.eventAttendedTotal === 5`
      - `users/A/profile/main.contentCompletedTotal === 3`
      - `users/A/profile/main.displayName === '...'` (preserved — merge worked)
      - `users/D/profile/main.eventAttendedTotal === 0`
    - Run script SECOND time → assert no diff (idempotent).
    - `--uid=A` mode: only User A's profile is touched; B–J unchanged.
  </behavior>
  <action>
    1. Verify `scripts/package.json` has `firebase-admin@^13.8.0`, `@firebase/rules-unit-testing@^5.0.0`, `vitest`, `tsx`. Add any missing.

    2. Author `scripts/__tests__/backfill-counters.test.ts` per <behavior>. Use `@firebase/rules-unit-testing` `initializeTestEnvironment()` to spin up the emulator-backed Firestore.

    3. Run the test → MUST FAIL because backfill-counters.ts does not yet exist (RED phase).

    4. Commit the test (RED commit): `test(02-14): add failing test for backfill-counters script`.
  </action>
  <verify>
    <automated>pnpm --filter @gamechangers/scripts test --run scripts/__tests__/backfill-counters.test.ts 2>&amp;1 | grep -E 'failed|FAIL'</automated>
  </verify>
  <done>
    Test file exists; test fails because the script under test is missing (proves test actually exercises the script, not a vacuous true).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement backfill script (GREEN) + author runbook</name>
  <files>
    scripts/backfill-counters.ts,
    docs/operations/backfill-counters-runbook.md
  </files>
  <read_first>
    scripts/__tests__/backfill-counters.test.ts (from Task 1 — implementation must satisfy this),
    scripts/cms-publish.ts (existing pattern for one-shot scripts in this repo — argv parsing, admin SDK init, exit codes)
  </read_first>
  <behavior>
    - CLI parsing: `--project=<id>` (required), `--dry-run` (optional, default false), `--uid=<uid>` (optional — single user), `--batch-size=<n>` (default 500).
    - Init admin SDK with `applicationDefault()` if `GOOGLE_APPLICATION_CREDENTIALS` is set, else require `--project` and use that. In emulator test mode, detect `FIRESTORE_EMULATOR_HOST` and skip credential bootstrap.
    - Algorithm:
      1. If `--uid` is set, target a single user; otherwise enumerate all uids by listing `users/` collection (paginated by `--batch-size`).
      2. For each uid:
         - Query `auditLog` (collection-group OR root-level — match what the production schema uses; `grep -r "auditLog" functions/ --include='*.ts'` to confirm).
         - Filter `where('uid', '==', uid)` AND `where('action', 'in', ['event_attended', 'content_completed'])`.
         - Aggregate counts per action.
         - Compute target update `{ eventAttendedTotal: N, contentCompletedTotal: M }`.
         - If `--dry-run`: print `{uid, current: {...}, target: {...}, diff: {...}}` JSON line.
         - Else: `db.doc(\`users/${uid}/profile/main\`).set(target, { merge: true })`.
      3. Track stats: total users scanned, users updated, total events processed, total content processed, errors.
      4. Print final summary JSON to stdout.
    - **Idempotency**: the script COMPUTES totals from auditLog (not increment), so re-running produces the same value. No "have we run before" guard needed.
    - **Performance**: batch profile reads via `getAll()` for the existing profile state; batch writes via `bulkWriter` (250 RPS limit for Firestore).
    - **Logging**: line-delimited JSON to stdout; one summary at end. No console formatting that breaks log aggregation.

    - **Author runbook** `docs/operations/backfill-counters-runbook.md`:
      - When to run: once per phase deploy that introduces new event_attended / content_completed action types; once on initial post-launch when historical users exist before counter denormalization.
      - Prerequisites: service account JSON with `roles/datastore.user` on the project; `tsx` + `pnpm` installed locally.
      - Commands:
        ```
        # Dry-run for safety:
        GOOGLE_APPLICATION_CREDENTIALS=./sa.json \
          pnpm --filter @gamechangers/scripts exec tsx backfill-counters.ts --project=gamechangers-prod --dry-run > backfill.preview.jsonl

        # Inspect, then run for real:
        GOOGLE_APPLICATION_CREDENTIALS=./sa.json \
          pnpm --filter @gamechangers/scripts exec tsx backfill-counters.ts --project=gamechangers-prod > backfill.run.jsonl
        ```
      - Expected runtime: ~1 minute per 1K users; ~10 minutes per 10K users (Firestore read pagination + write throttle).
      - Rollback: there isn't one — counters are deterministic. If a bad run is suspected, re-run; results converge to the same totals.
  </behavior>
  <action>
    1. Author `scripts/backfill-counters.ts` per <behavior>. Style: TypeScript with explicit types; no `any`. Use `commander` or simple `process.argv` parsing (the repo's other scripts likely choose one — match the existing pattern).

    2. Run the test from Task 1 → MUST PASS (GREEN phase).

    3. If any test case fails, fix the script (do NOT modify the test). Re-run.

    4. Commit (GREEN commit): `feat(02-14): implement backfill-counters script`.

    5. Author `docs/operations/backfill-counters-runbook.md` per <behavior>.

    6. Optional REFACTOR: extract the aggregation logic into a pure function `aggregateAuditLog(entries: AuditLogEntry[]): { events: number; content: number }` for clarity. If applied, run tests again (must still pass) and commit `refactor(02-14): extract aggregation function`.
  </action>
  <verify>
    <automated>pnpm --filter @gamechangers/scripts test --run scripts/__tests__/backfill-counters.test.ts</automated>
  </verify>
  <done>
    Test from Task 1 passes; script is invokable; runbook exists; idempotency proven by the test re-running the script and asserting no diff.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backfill script → production Firestore | Script holds Datastore User credentials; can write any profile doc |
| auditLog → profile counters | One-way derivation; auditLog is source of truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-14-01 | Tampering | Script writes wrong counter via aggregation bug | MEDIUM | mitigate | Task 1: emulator-backed unit test with hand-computed expected values for 10 users |
| T-02-14-02 | Tampering | Script overwrites unrelated profile fields | HIGH | mitigate | Task 2 spec: `set(..., { merge: true })`; test asserts displayName preserved post-run |
| T-02-14-03 | Information Disclosure | Service account key persisted in repo or shell history | HIGH | accept | Runbook documents key handling; reduce by using `gcloud auth print-access-token` impersonation pattern as alternative; do NOT commit keys |
| T-02-14-04 | DoS | Script reads collide with live writes during scan | LOW | accept | Read-heavy work; Firestore is consistent under read fanout; consider running during low-traffic window |
</threat_model>

<verification>
- `pnpm --filter @gamechangers/scripts test --run scripts/__tests__/backfill-counters.test.ts` exits 0.
- Test asserts: dry-run produces no writes; real run produces correct counts; second run is no-op (idempotent); merge preserves other fields.
- `docs/operations/backfill-counters-runbook.md` exists with the documented commands.
</verification>

<success_criteria>
- G3 closed: backfill script exists, tested, idempotent, documented.
- Future onboarding of pre-WR-06 historical users will have correct denormalized counters.
</success_criteria>

<output>
After completion, create `.planning/phases/02-platform-mvp/02-14-SUMMARY.md` documenting:
- Aggregation source (auditLog collection name + action strings)
- Idempotency proof (test scenario)
- Runbook reference
- One-line example invocation
</output>
