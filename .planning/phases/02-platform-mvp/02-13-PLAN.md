---
phase: 02-platform-mvp
plan: 13
type: execute
wave: 8
depends_on: []
gap_closure: true
files_modified:
  - apps/pwa/package.json
  - apps/pwa/src/views/events/EventList.vue
  - apps/pwa/src/views/me/Badges.vue
  - apps/pwa/src/views/events/EventDetail.vue
  - docs/architecture/ADR-012-vite-version-decision.md
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
gap_closure_targets:
  - G4

# Revision history:
# - rev-1 (2026-05-09): Fixed Blocker-5 (Vite 8 decision rule was vacuous — necessary
#   baselines for ANY Vite version proved nothing about 8 vs 7). Decision rule rewritten:
#   default branch is now DOWNGRADE per CLAUDE.md guidance ("Pin to ^7.4 unless you've
#   validated v8 with all plugins"); to KEEP Vite 8, operator must produce affirmative
#   evidence of plugin compatibility (URLs, dates, screenshots) recorded in ADR-012.

must_haves:
  truths:
    - "`pnpm --filter @gamechangers/pwa build` exits 0 (no TypeScript errors, no Vite errors)"
    - "`@types/qrcode` is in `apps/pwa/package.json` devDependencies; `qrcode` import in `EventDetail.vue:264` is type-checked"
    - "`apps/pwa/src/views/events/EventList.vue:43` no longer fails TypeScript: DocumentData is properly cast to the expected EventData type (or the explicit type parameter is supplied to the VueFire helper)"
    - "`apps/pwa/src/views/me/Badges.vue` lines around 67-69 have null guards on the 3 fields that were tripping `Object is possibly 'undefined'` errors"
    - "Vite version decision is documented in `docs/architecture/ADR-012-vite-version-decision.md` with EXPLICIT plugin-compatibility evidence: either (a) KEEP Vite 8.0.10 with affirmative evidence (vite-plugin-pwa@1.2.0 issue/test confirming Vite 8 support; @vitejs/plugin-vue@6.0.6 with no Vite-8 incompatibility issues open in past 90 days; URLs + dates + screenshots inline in ADR), or (b) DOWNGRADE to ^7.4 per CLAUDE.md (default branch — burden is on KEEP, not DOWNGRADE)"
    - "Bundle metric captured: `dist/` total gzipped size on public routes is recorded in the ADR (target ≤200KB per A11Y-02)"
    - "Service worker generates: `dist/sw.js` exists post-build"
  artifacts:
    - path: "apps/pwa/package.json"
      provides: "Updated devDependencies (`@types/qrcode`) and possibly `vite` version pin"
    - path: "docs/architecture/ADR-012-vite-version-decision.md"
      provides: "Decision record for Vite 8 vs 7.4 with plugin-compatibility evidence"
  key_links:
    - from: "EventDetail.vue:264 import('qrcode')"
      to: "@types/qrcode"
      via: "TypeScript declaration resolution"
      pattern: "@types/qrcode"
    - from: "vite build pipeline"
      to: "vite-plugin-pwa generateSW + workbox"
      via: "vite.config.ts plugins array"
      pattern: "VitePWA"
---

<objective>
Get `pnpm --filter @gamechangers/pwa build` from "fails on 4+ TypeScript errors" to "exits 0 with measured bundle". The PWA build is currently broken on type errors unrelated to Vite itself: missing `@types/qrcode`, an unsafe DocumentData cast in `EventList.vue`, and 3 null-guard misses in `Badges.vue`. Once these are fixed, capture bundle size + verify service worker generation. Then make a documented decision on Vite 8.0.10 vs ^7.4: per CLAUDE.md ("Pin to ^7.4 unless you've validated v8 with all plugins"), the burden of proof is on KEEP, not DOWNGRADE. Default branch is DOWNGRADE unless operator can produce affirmative ecosystem-validation evidence (vite-plugin-pwa@1.2.0 + @vitejs/plugin-vue@6.0.6 confirmed Vite-8-compatible).

Purpose: Closes G4 (MEDIUM) from `02-HUMAN-UAT.md`. Without a green build, A11Y-02 (<3s on 3G) cannot be measured against any Vite output, and bundle-size discipline (≤200KB gzipped on public routes) is unverifiable. Pure code work; no infrastructure dependency; can run any time.

Output: `pnpm --filter @gamechangers/pwa build` exits 0; `dist/sw.js` exists; bundle size recorded in ADR-012; Vite version decision committed with EXPLICIT plugin-compatibility evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-platform-mvp/02-HUMAN-UAT.md
@.planning/phases/02-platform-mvp/02-VERIFICATION.md
@CLAUDE.md
@apps/pwa/package.json
@apps/pwa/src/views/events/EventList.vue
@apps/pwa/src/views/me/Badges.vue
@apps/pwa/src/views/events/EventDetail.vue
@apps/pwa/vite.config.ts

<interfaces>
```typescript
// EventList.vue:43 — current pattern (failing):
//   const events = useCollection<DocumentData>(query)  // OR similar without typed projection

// Target shape: events should be typed as EventData[]:
interface EventData {
  id: string;
  title: string;
  city: 'Quito' | 'Guayaquil' | 'Cuenca';
  startsAt: Timestamp;
  capacityMax: number;
  // ... rest
}
// Fix: pass explicit generic, or use VueFire `.withConverter` pattern for runtime+compile-time safety.

// Badges.vue:67-69 — current pattern (failing):
//   const url = badge.imageUrl;  // Object is possibly 'undefined'
//   const provenance = badge.provenance.signature;  // chained access
// Fix: optional-chaining + default values, OR refine the source type so these fields are required.

// EventDetail.vue:264 — current pattern (failing):
//   const { default: QRCode } = await import('qrcode');  // no types
// Fix: install @types/qrcode (the qrcode npm package ships untyped).
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install @types/qrcode + fix EventList.vue + Badges.vue type errors; capture build evidence</name>
  <files>
    apps/pwa/package.json,
    apps/pwa/src/views/events/EventList.vue,
    apps/pwa/src/views/me/Badges.vue,
    apps/pwa/src/views/events/EventDetail.vue
  </files>
  <read_first>
    apps/pwa/src/views/events/EventList.vue (lines 1-60: identify the line 43 cast),
    apps/pwa/src/views/me/Badges.vue (lines 60-80: 3 errors at 67-69),
    apps/pwa/src/views/events/EventDetail.vue (line 264 — qrcode import),
    apps/pwa/tsconfig.json (confirm `noImplicitAny`, `strictNullChecks` are on)
  </read_first>
  <behavior>
    - `EventDetail.vue:264` `await import('qrcode')` must resolve types via `@types/qrcode` (no ambient declaration shim — install the proper @types).
    - `EventList.vue:43` should produce a strongly-typed `EventData[]` array; if VueFire is used, prefer `useCollection<EventData>(...)` or attach a Firestore `.withConverter()` so the inferred type is correct without explicit casts. Avoid `as any`. If a one-off cast is unavoidable, scope it narrowly and add a comment explaining why.
    - `Badges.vue` lines 67-69: each access of an optional property must use `?.` or be guarded by an `if`. Default values where appropriate (e.g., `badge.imageUrl ?? '/img/badge-placeholder.png'`).
    - Build must exit 0. If any new TS errors surface during the fix process, fix them inline.
  </behavior>
  <action>
    1. `pnpm --filter @gamechangers/pwa add -D @types/qrcode`. Verify version aligns with `qrcode` runtime version (qrcode 1.5.4 -> @types/qrcode ^1.5.x).

    2. `pnpm --filter @gamechangers/pwa build` — capture the EXACT TS error output. Save to `/tmp/pwa-build-errors.txt` for reference (do not commit).

    3. Read `EventList.vue` lines around 43 and the surrounding `<script setup>` to determine the source of the DocumentData type. Apply ONE of the following fixes (whichever matches existing convention in other PWA views — check `EventDetail.vue` and other views for precedent):
       - Pattern A (preferred): `useCollection<EventData>(...)` with `EventData` imported from a shared type module (likely `apps/pwa/src/types/event.ts` if it exists; otherwise create it).
       - Pattern B: Apply `.withConverter<EventData>(eventConverter)` on the Firestore query before passing to VueFire.
       - Pattern C (fallback only): explicit `as EventData[]` cast with a comment `// SAFE: schema enforced by Firestore Rules + zod on write — see functions/events/src/createEvent.ts`.

    4. Read `Badges.vue` lines 60-80. Identify the 3 specific fields tripping the error. For each, apply `?.` or default-value guard. Run `pnpm --filter @gamechangers/pwa build` after each change to confirm fewer errors remain.

    5. Verify `EventDetail.vue:264` no longer errors after `@types/qrcode` install.

    6. `pnpm --filter @gamechangers/pwa build` exits 0. Capture the build output (size table) — vite reports per-asset sizes; record this for Task 2.

    7. Run existing tests to ensure the typing fixes didn't break behavior: `pnpm --filter @gamechangers/pwa test`.
  </action>
  <verify>
    <automated>pnpm --filter @gamechangers/pwa build &amp;&amp; test -f apps/pwa/dist/sw.js</automated>
  </verify>
  <done>
    Build exits 0; service worker generated; all 4 type errors resolved; existing tests still pass; per-asset bundle sizes captured for ADR.
  </done>
</task>

<task type="auto">
  <name>Task 2: Decide Vite 8 vs ^7.4 with affirmative plugin-compatibility evidence (default = DOWNGRADE per CLAUDE.md)</name>
  <files>
    docs/architecture/ADR-012-vite-version-decision.md,
    apps/pwa/package.json
  </files>
  <read_first>
    CLAUDE.md (Technology Stack table — Vite row says "Pin to ^7.4 unless you've validated v8 with all plugins"),
    apps/pwa/package.json (verify current vite version + plugin versions: vite-plugin-pwa, @vitejs/plugin-vue),
    .planning/phases/02-platform-mvp/02-01-SUMMARY.md (the original Plan 01 deviation note)
  </read_first>
  <behavior>
    - Decision is binary, binding, AND has an asymmetric burden of proof:
      - **Default = Decision B: DOWNGRADE to ^7.4** (per CLAUDE.md). Required action: `pnpm --filter @gamechangers/pwa add vite@^7.4`; re-run build + tests; verify all artifacts still produce.
      - **Decision A: KEEP Vite 8.0.10** is allowed ONLY if the operator can produce ALL of the following affirmative evidence inline in the ADR:
        1. **vite-plugin-pwa Vite 8 support evidence**: a passing test or upstream issue/PR/release note explicitly confirming `vite-plugin-pwa@1.2.0` works with Vite 8. Acceptable artifacts: a green CI run on the vite-plugin-pwa repo with Vite 8 in the matrix; an issue/PR with maintainer confirmation; a release-notes line "supports Vite 8". URL + accessed-on date + screenshot pasted inline.
        2. **@vitejs/plugin-vue Vite 8 compatibility evidence**: zero open Vite-8-incompatibility issues against `@vitejs/plugin-vue@6.0.6` in the past 90 days. Operator runs the search: https://github.com/vitejs/vite-plugin-vue/issues?q=is%3Aissue+vite+8 — pastes the result count + date + a representative screenshot.
        3. **Local proof**: the build with Vite 8 exits 0 (already established by Task 1) AND there are zero Vite-8-specific deprecation warnings in the build output (operator captures the full build log and pastes the relevant warnings or a "no warnings" assertion into the ADR).
      - Without ALL THREE pieces of affirmative evidence, the decision MUST be DOWNGRADE.
    - Whichever decision is taken, the ADR documents:
      - Build size on public routes (target: ≤200KB gzipped per A11Y-02).
      - Service worker generation confirmed.
      - Plugin compatibility list with exact versions.
      - Future trigger to re-evaluate (e.g., "re-evaluate at next Vue / Vite major").
  </behavior>
  <action>
    1. Author `docs/architecture/ADR-012-vite-version-decision.md` skeleton with:
       - Title: "ADR-012: Vite Version — Keep 8.0.10 vs Downgrade to ^7.4"
       - Status: Proposed -> Accepted (after this task completes)
       - Context: CLAUDE.md guidance to pin ^7.4; Plan 01 deviation to 8.0.10 documented; G4 in 02-HUMAN-UAT requires resolution; per CLAUDE.md the burden is on KEEP.
       - Options A and B as above, with Option B clearly marked as the default.

    2. Run the basic build evidence (necessary for ANY decision branch — these prove the build works, not that 8 vs 7 was right):
       - `pnpm --filter @gamechangers/pwa build` — record build time + output table. Capture FULL build output to a temp file for warning analysis.
       - Compute gzipped size for public-route assets: `gzip -c apps/pwa/dist/assets/*.js | wc -c` aggregated. (Or use vite-bundle-visualizer if installed.)
       - Verify `dist/sw.js` exists and `dist/manifest.webmanifest` exists.
       - `pnpm --filter @gamechangers/pwa test` — exits 0.

    3. **Decision rule (Blocker-5 fix)**: DEFAULT IS DOWNGRADE. Operator may KEEP only if all three pieces of affirmative evidence are produced.

       **To attempt Decision A (KEEP Vite 8.0.10)**, operator must:
       - **Evidence 1 — vite-plugin-pwa Vite 8 support**: visit https://github.com/vite-pwa/vite-plugin-pwa (releases + issues + CI matrix). Find a positive signal that 1.2.0 works on Vite 8. Paste URL + date + relevant excerpt or screenshot path into ADR §"Evidence 1".
       - **Evidence 2 — @vitejs/plugin-vue Vite 8 compatibility**: visit https://github.com/vitejs/vite-plugin-vue/issues?q=is%3Aissue+vite+8 . Confirm zero open Vite-8-incompatibility issues in past 90 days against 6.0.6. Paste URL + date + result count screenshot into ADR §"Evidence 2".
       - **Evidence 3 — local zero deprecation warnings**: review the full build log captured in step 2. Search for "deprecat" / "warning" / "WARN". Paste the full warning section (or "no warnings emitted") into ADR §"Evidence 3".

       If ANY of the three pieces of evidence is missing, fail-safe to DOWNGRADE:
       - `pnpm --filter @gamechangers/pwa add vite@^7.4.0`
       - `pnpm --filter @gamechangers/pwa build` again — capture output.
       - Verify all artifacts produce; tests pass.
       - Write the ADR documenting the downgrade rationale: "Insufficient affirmative evidence to override CLAUDE.md `Pin to ^7.4` default; downgrading per project standard."

    4. If decision is KEEP after producing all three pieces of evidence: write the ADR with that evidence inline; pin `"vite": "^8.0.10"` (caret allows minor patches but not 9.x); commit. Note in ADR that re-evaluation trigger is "any of the three evidence sources changes status".

    5. If decision is DOWNGRADE: write the ADR documenting which evidence was missing AND the downgrade outcome (build green, bundle size, tests).

    6. Status the ADR as `Accepted` with date.

    7. Commit. Note in the SUMMARY which decision branch was taken AND, if KEEP, link to the three pieces of evidence captured.
  </action>
  <verify>
    <automated>pnpm --filter @gamechangers/pwa build &amp;&amp; test -f apps/pwa/dist/sw.js &amp;&amp; test -f docs/architecture/ADR-012-vite-version-decision.md &amp;&amp; grep -q 'Accepted' docs/architecture/ADR-012-vite-version-decision.md &amp;&amp; grep -qE '(Evidence 1|Insufficient affirmative evidence)' docs/architecture/ADR-012-vite-version-decision.md</automated>
  </verify>
  <done>
    ADR-012 exists with status Accepted; binding decision recorded — if KEEP, all three pieces of affirmative plugin-compatibility evidence are pasted inline (URLs, dates, screenshots); if DOWNGRADE, the missing evidence is named explicitly; package.json reflects the chosen version; build green and SW generated.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline -> production artifacts | TypeScript errors -> broken runtime; loose `as any` casts hide schema drift |
| PWA service worker -> user device | SW must generate; missing SW means no offline support -> A11Y/Pitfall regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-13-01 | Tampering | Loose `as any` cast hides Firestore schema drift | LOW | mitigate | Task 1: prefer typed VueFire generic or .withConverter() over `as any` |
| T-02-13-02 | DoS | Vite 8 produces larger bundle -> 3G perf regression | LOW | mitigate | Task 2: explicit gzip-size check against 200KB budget; downgrade if exceeded |
| T-02-13-03 | Tampering | Vite 8 silently regresses plugin behavior at runtime | MEDIUM | mitigate | Task 2: affirmative plugin-compatibility evidence required to KEEP; default-branch is DOWNGRADE per CLAUDE.md |
</threat_model>

<verification>
- `pnpm --filter @gamechangers/pwa build` exits 0.
- `apps/pwa/dist/sw.js` exists.
- `@types/qrcode` in package.json devDependencies.
- ADR-012 exists with Accepted status and gzipped bundle size recorded.
- ADR-012 explicitly names the decision (KEEP / DOWNGRADE) with the corresponding evidence section (three pieces of affirmative evidence for KEEP, or named-missing-evidence for DOWNGRADE).
- Existing PWA test suite passes after fixes.
</verification>

<success_criteria>
- G4 closed: build green, type errors fixed, Vite version decision committed with proper burden-of-proof discipline.
- A11Y-02 measurement (≤200KB on public routes) is now possible against the produced bundle.
- CLAUDE.md `Pin to ^7.4 unless validated` rule is honored.
</success_criteria>

<output>
After completion, create `.planning/phases/02-platform-mvp/02-13-SUMMARY.md` documenting:
- The 4 type errors and their fixes (one-line each)
- Vite version decision (Keep vs Downgrade) with the trigger evidence (or missing-evidence note)
- Gzipped bundle size measured
- Reference to ADR-012
</output>
</content>
</invoke>