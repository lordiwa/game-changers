---
phase: 02-platform-mvp
plan: 09
type: execute
wave: 7
depends_on: [02-08]
files_modified:
  - functions/wearables/src/index.ts
  - functions/wearables/src/openWearablesWebhook.ts
  - functions/wearables/src/aggregateDailyHealth.ts
  - functions/wearables/src/disconnectDevice.ts
  - functions/wearables/src/connectDevice.ts
  - functions/wearables/src/__tests__/openWearablesWebhook.test.ts
  - functions/wearables/src/__tests__/aggregateDailyHealth.test.ts
  - functions/wearables/src/__tests__/disconnectDevice.test.ts
  - functions/wearables/src/__tests__/no-medical-alerts.test.ts
  - functions/wearables/package.json
  - functions/b2b/src/index.ts
  - functions/b2b/src/partnerEmbedJwt.ts
  - functions/b2b/src/__tests__/partnerEmbedJwt.test.ts
  - functions/b2b/package.json
  - apps/pwa/src/views/wearables/Wearables.vue
  - apps/pwa/src/views/wearables/ConnectDevice.vue
  - apps/pwa/src/components/WearableProviderCard.vue
  - apps/pwa/src/composables/useWearables.ts
  - apps/pwa/src/router/index.ts
  - apps/pwa/src/locales/es.json
  - apps/pwa/src/locales/en.json
  - apps/pwa/src/__tests__/useWearables.test.ts
  - tests/rules/wearables-data-consent.test.ts
  - tests/bigquery/anonymized-views.test.sql
  - bigquery/views/active_movers_by_city.sql
  - bigquery/views/social_connectors_by_age_band.sql
  - bigquery/views/competitive_core_by_cluster.sql
  - bigquery/views/new_recruits.sql
  - bigquery/views/at_risk_segment.sql
  - bigquery/setup/create-datasets.sh
  - bigquery/setup/grant-metabase-readonly.sh
  - bigquery/setup/revoke-raw-access.sh
  - docs/architecture/ADR-011-wearables-fork-strategy.md
  - docs/wearables/open-wearables-deploy.md
  - functions/consent/src/wearableRevokeHandler.ts
  - firestore.indexes.json
autonomous: false
requirements:
  - WEAR-01
  - WEAR-02
  - WEAR-03
  - WEAR-04
  - WEAR-05
  - WEAR-06
  - WEAR-07
  - WEAR-08
  - WEAR-09
  - WEAR-10
  - WEAR-11
  - WEAR-12
user_setup:
  - service: open-wearables
    why: "Self-hosted FastAPI wearables hub deployment is OS-level work outside Firebase scope"
    env_vars:
      - name: OW_HMAC_SECRET
        source: "Generated during Open Wearables deployment; stored in GCP Secret Manager"
      - name: OPEN_WEARABLES_BASE_URL
        source: "Public HTTPS URL of the deployed FastAPI hub (e.g., https://ow.gamechangers.gg)"
    dashboard_config:
      - task: "Deploy Open Wearables 0.4.3 (Python/FastAPI) on Hetzner CX22 (Falkenstein) via Docker Compose; configure outgoing webhook to https://southamerica-east1-<project>.cloudfunctions.net/wearables-openWearablesWebhook"
        location: "Hetzner Cloud Console + the-momentum/open-wearables Docker Compose"
      - task: "Register OAuth apps with each wearable provider (Garmin Connect IQ, Fitbit, Polar AccessLink, Whoop, Oura Cloud) per Open Wearables provider docs"
        location: "Each provider's developer portal"
      - task: "Configure provider HMAC signing key in Open Wearables; share same secret with our Cloud Function via OW_HMAC_SECRET"
        location: "Open Wearables admin UI + GCP Secret Manager"
  - service: bigquery
    why: "BigQuery dataset gw_b2b_views requires schema-level k-anonymity views; setup is one-time SQL operation"
    env_vars:
      - name: BIGQUERY_DATASET_RAW
        source: "Default 'gw_analytics' (created in Plan 01 when Firestore→BigQuery extension was installed)"
      - name: BIGQUERY_DATASET_VIEWS
        source: "'gw_b2b_views' (created in this plan)"
      - name: METABASE_SERVICE_ACCOUNT_EMAIL
        source: "Created here as metabase-readonly@<project>.iam.gserviceaccount.com"
    dashboard_config:
      - task: "Run bigquery/setup/create-datasets.sh + grant-metabase-readonly.sh + revoke-raw-access.sh"
        location: "GCP Console > BigQuery (or via gcloud CLI)"

must_haves:
  truths:
    - "Open Wearables webhook receives normalized samples from Garmin/Fitbit/Polar/Whoop/Oura; HMAC validated with timing-safe comparison BEFORE any DB write"
    - "Webhook checks active wearable_data consent before persisting any sample (WEAR-05)"
    - "Wearable samples normalized to common schema {metric, value, unit, recordedAt, source} (WEAR-06)"
    - "Time-series storage: /users/{uid}/healthSamples/{yyyy-mm}/metrics/{id} (monthly buckets per WEAR-07 + RESEARCH §4)"
    - "Daily rollup: /users/{uid}/healthDaily/{yyyy-mm-dd} maintained by debounced onWrite trigger (WEAR-08)"
    - "Only healthDaily exported to BigQuery; healthSamples NEVER exported (WEAR-09 + Pitfall #6 + Plan 01 ADR-009)"
    - "User can disconnect wearable; data retained per consent or deleted on explicit request (WEAR-10)"
    - "Wearable data drives challenge auto-tracking but is NEVER required to participate (WEAR-11 + Pitfall #9 — manual-first from Plan 08)"
    - "NO automated medical alerts from wearable patterns (WEAR-12 anti-feature); concerning patterns surface relevant content + crisis resources only"
    - "Native Apple HealthKit + Android Health Connect explicitly DEFERRED to Phase 3 per D-11; Phase 2 webhook-only"
    - "TTL policy active on /users/{uid}/healthSamples: expireAt = recordedAt + 90 days (RESEARCH §4 LOPDP minimization)"
    - "BigQuery anonymized views in dataset gw_b2b_views with k≥50 (HAVING COUNT(DISTINCT user_id) >= 50) AND ε-DP (WITH DIFFERENTIAL_PRIVACY OPTIONS(epsilon=1.0))"
    - "5 pre-built audience segments: Active Movers, Social Connectors, Competitive Core, New Recruits, At Risk (matching B2BD-08 spec from Phase 3)"
    - "Schema-level generalization: age_band 5-year buckets; region collapse for cohorts <500; is_minor=FALSE filter on every B2B view (D-14)"
    - "Metabase service account has SELECT on gw_b2b_views ONLY; ZERO access to gw_analytics raw mirror (verified in tests/bigquery/anonymized-views.test.sql)"
    - "Open Wearables fork strategy decision documented in ADR-011 (WEAR-04)"
  artifacts:
    - path: "functions/wearables/src/openWearablesWebhook.ts"
      provides: "HMAC-validated webhook + consent gate + monthly bucket write"
      exports: ["openWearablesWebhook"]
    - path: "functions/wearables/src/aggregateDailyHealth.ts"
      provides: "onDocumentWritten trigger + Pub/Sub-debounced daily rollup"
      exports: ["aggregateDailyHealth"]
    - path: "functions/wearables/src/disconnectDevice.ts"
      provides: "User-initiated wearable disconnect"
      exports: ["disconnectDevice"]
    - path: "bigquery/views/active_movers_by_city.sql"
      provides: "B2B view: Active Movers segment with k≥50 + ε-DP"
    - path: "bigquery/setup/revoke-raw-access.sh"
      provides: "Script revoking Metabase service-account access to gw_analytics raw mirror"
    - path: "docs/architecture/ADR-011-wearables-fork-strategy.md"
      provides: "Fork strategy for Open Wearables 0.4.3 pre-1.0 maintenance risk (WEAR-04)"
    - path: "apps/pwa/src/views/wearables/Wearables.vue"
      provides: "User-facing wearable connect/disconnect surface"
  key_links:
    - from: "functions/wearables/src/openWearablesWebhook.ts"
      to: "functions/shared/hmac.ts (verifyHmacSha256)"
      via: "HMAC + timingSafeEqual on raw body"
      pattern: "verifyHmacSha256.*OW_HMAC_SECRET"
    - from: "functions/wearables/src/openWearablesWebhook.ts"
      to: "functions/shared/ConsentEnforcement.ts (consentGate wearable_data)"
      via: "Reject 403 if consent revoked"
      pattern: "consentGate.*wearable_data"
    - from: "bigquery/views/*.sql"
      to: "gw_analytics.*_changelog tables"
      via: "JOIN on user_id; HAVING k>=50; WITH DIFFERENTIAL_PRIVACY epsilon=1.0"
      pattern: "HAVING COUNT\\(DISTINCT user_id\\) >= 50"
---

<objective>
Last in Phase 2 per the dependency-locked build order: integrate Open Wearables 0.4.3 (self-hosted FastAPI on a Hetzner VPS) via webhook with HMAC validation + consent gate, build the monthly-bucketed time-series schema (`/users/{uid}/healthSamples/{yyyy-mm}/metrics/{id}`) with Pub/Sub-debounced daily rollups (`/users/{uid}/healthDaily/{yyyy-mm-dd}`) per RESEARCH §4, configure the BigQuery anonymized two-tier B2B pipeline (raw `gw_analytics` mirror DPO-only + `gw_b2b_views` k≥50 + ε-DP views), publish 5 pre-built audience segments (Active Movers / Social Connectors / Competitive Core / New Recruits / At Risk) used in Phase 3 dashboards, build the user-facing wearable connect/disconnect UI, and explicitly DEFER Apple HealthKit + Android Health Connect native integrations to Phase 3 (D-11 — webhook-only in Phase 2). Document the Open Wearables fork strategy (WEAR-04). Block automated medical alerts (WEAR-12 anti-feature).

Purpose: This is the LAST step in Phase 2's dependency chain because it requires consent (Plan 04), profile/character sheet (Plan 05), challenges (Plan 08), and event auth (Plan 02) all to be solid. Wearables auto-track challenge progress when connected (WEAR-11) but NEVER gate features (Pitfall #9 already mitigated by Plans 05 + 08's manual-first design). The two-tier B2B architecture is built but unexposed: per Pitfall #6, retrofitting in Phase 3 costs 5–10x — building it now even with no partner connected is the discipline.

Output: A user can connect Garmin/Fitbit/Polar/Whoop/Oura via Open Wearables OAuth flow → samples stream to webhook → daily rollup feeds character sheet stats and challenge progress → BigQuery views are queryable by Metabase service account with k≥50 enforced AND raw access revoked (verified by SQL test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-platform-mvp/02-CONTEXT.md
@.planning/phases/02-platform-mvp/02-RESEARCH.md
@.planning/phases/02-platform-mvp/02-UI-SPEC.md
@.planning/phases/02-platform-mvp/02-04-PLAN.md
@.planning/phases/02-platform-mvp/02-05-PLAN.md
@.planning/phases/02-platform-mvp/02-08-PLAN.md
@.planning/research/PITFALLS.md (Pitfall #6 + #9)
@CLAUDE.md

<interfaces>
```typescript
export interface HealthSample {
  metric: 'steps' | 'heart_rate' | 'sleep_minutes' | 'calories' | 'workout' | 'distance';
  value: number;
  unit: string;
  recordedAt: Timestamp;
  source: 'open-wearables' | 'phone-pedometer';
  providerName?: 'garmin' | 'fitbit' | 'polar' | 'whoop' | 'oura';
  expireAt: Timestamp;       // recordedAt + 90d for TTL policy
}
export interface HealthDaily {
  date: string;              // 'yyyy-mm-dd'
  stepsTotal: number;
  hrAvg: number | null;
  hrMax: number | null;
  hrRest: number | null;
  sleepMinutes: number | null;
  caloriesActive: number | null;
  workoutCount: number;
  byMetric: Record<string, { sum: number; count: number; avg: number }>;
  computedAt: Timestamp;
}
// Open Wearables webhook payload (FastAPI server posts):
type OpenWearablesWebhookPayload = {
  uid: string;                // mapped from provider userId via /users/_lookup/wearable/{providerId}
  provider: 'garmin'|'fitbit'|'polar'|'whoop'|'oura';
  samples: Array<{
    id: string;
    metric: string;
    value: number;
    unit: string;
    recordedAt: string;       // ISO 8601
  }>;
};
// BigQuery view contract (Phase 3 will build dashboards on top):
// gw_b2b_views.active_movers_by_city: city, active_user_count (k>=50), avg_challenges, avg_steps_7d
// gw_b2b_views.social_connectors_by_age_band: age_band, connector_count, avg_events_per_month
// ... (5 segments total)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cloud Functions — Open Wearables webhook (HMAC + consent gate + monthly bucket write), debounced daily rollup, disconnect handler, no-medical-alerts assertion + ADR-011 fork strategy</name>
  <files>
    functions/wearables/src/index.ts,
    functions/wearables/src/openWearablesWebhook.ts,
    functions/wearables/src/aggregateDailyHealth.ts,
    functions/wearables/src/disconnectDevice.ts,
    functions/wearables/src/connectDevice.ts,
    functions/wearables/src/__tests__/openWearablesWebhook.test.ts,
    functions/wearables/src/__tests__/aggregateDailyHealth.test.ts,
    functions/wearables/src/__tests__/disconnectDevice.test.ts,
    functions/wearables/src/__tests__/no-medical-alerts.test.ts,
    functions/wearables/package.json,
    functions/consent/src/wearableRevokeHandler.ts,
    docs/architecture/ADR-011-wearables-fork-strategy.md,
    docs/wearables/open-wearables-deploy.md,
    firestore.indexes.json,
    tests/rules/wearables-data-consent.test.ts
  </files>
  <read_first>
    .planning/phases/02-platform-mvp/02-RESEARCH.md (§4 wearable time-series schema IN FULL — Code Example 4 monthly bucket + daily rollup; §Security HMAC timing attack; Open Wearables webhook docs),
    .planning/phases/02-platform-mvp/02-CONTEXT.md (D-11 webhook-only, native HealthKit/Health Connect deferred to Phase 3),
    functions/shared/hmac.ts (from Plan 01 — verifyHmacSha256 timing-safe),
    functions/shared/ConsentEnforcement.ts (from Plan 01),
    functions/consent/src/revoke.ts (from Plan 04 — publishes consent-revoked Pub/Sub),
    https://github.com/the-momentum/open-wearables (Open Wearables 0.4.3 webhook payload spec)
  </read_first>
  <behavior>
    - **`openWearablesWebhook`** (HTTP onRequest, region southamerica-east1, secrets `OW_HMAC_SECRET`):
        1. Read `X-OW-Signature` header. Read raw body via `req.rawBody`.
        2. `verifyHmacSha256(req.rawBody, signature, OW_HMAC_SECRET)` — if false, 401. **CRITICAL**: timing-safe comparison via Plan 01's `crypto.timingSafeEqual`.
        3. zod-validate `OpenWearablesWebhookPayload`.
        4. Resolve uid: read `/users/_lookup/wearable/{provider}/{providerId}` mapping created by `connectDevice`. If missing, 404.
        5. `consentGate(uid, 'wearable_data')` — if missing/revoked, 403 (and Open Wearables should detect 403 and stop sending; an additional belt-and-braces is the consent revoke handler in Plan 04 that calls `disconnectDevice` server-side).
        6. **Anti-cheat pre-check** (per Plan 05's antiCheat module): for each sample, check withinAntiCheatBounds. If REJECT, drop the sample with audit `{ action: 'wearable_sample_rejected', reason }`. If FLAG, store in private flagged collection.
        7. Batch write samples: for each `s in samples`:
            - Compute `yyyymm = s.recordedAt.slice(0, 7)`.
            - Compute `expireAt = new Date(s.recordedAt) + 90d`.
            - Doc path `/users/{uid}/healthSamples/{yyyymm}/metrics/{s.id}` (deterministic ID via provider's sample ID — replay-safe).
        8. After commit, schedule a Cloud Task delayed 60s calling `aggregateDailyHealth` for each (uid, yyyy-mm-dd) touched. The Task body is `{ uid, day }`. Cloud Tasks queue named `daily-health-rollup` with rate-limit 100 RPS to throttle Garmin sync bursts (2K samples in 5s per RESEARCH §4).
        9. Return 204.

    - **`aggregateDailyHealth`** (Cloud Task target HTTP onRequest):
        1. zod-validate `{ uid, day: yyyy-mm-dd }`.
        2. Query `/users/{uid}/healthSamples/{yyyymm}/metrics` filtered by `recordedAt within day`.
        3. Aggregate per metric: sum/count/avg for each.
        4. Write `/users/{uid}/healthDaily/{yyyy-mm-dd}` with computed values.
        5. Audit log.

    - **`connectDevice`** (HTTPS callable):
        1. zod-validate `{ provider }`.
        2. `consentGate(uid, 'wearable_data')`.
        3. Generate state + return Open Wearables OAuth URL: `${OPEN_WEARABLES_BASE_URL}/connect/${provider}?uid=${uid}&state=...&callback=https://gamechangers.gg/me/wearables/callback`.
        4. PWA opens this URL; OW handles provider OAuth; OW posts back to our webhook with samples once authorization succeeds.
        5. Returns `{ ok, oauthUrl }`.

    - **`disconnectDevice`** (HTTPS callable, also called by `wearableRevokeHandler` in functions/consent on consent-revoked event):
        1. zod-validate `{ provider, retainData: boolean }`.
        2. POST to OW `${OPEN_WEARABLES_BASE_URL}/disconnect/{uid}/{provider}` to stop sample stream (idempotent — safe to call even if already disconnected).
        3. Delete `/users/{uid}/_lookup/wearable/{provider}/{providerId}`.
        4. If `retainData=false`: queue Cloud Task to recursively delete `/users/{uid}/healthSamples` AND `/users/{uid}/healthDaily` (per WEAR-10).
        5. Audit log.

    - **`wearableRevokeHandler`** (functions/consent, `onMessagePublished({ topic: 'consent-revoked' })`): when `consent-revoked` fires for `category == 'wearable_data'`, automatically call `disconnectDevice({ retainData: true })` server-side. Plan 04 already publishes this topic; this is the consumer. RetainData defaults to true; user can request hard-delete via the UI separately.

    - **TTL policy**: configure Firestore TTL on collection-group `metrics` with field `expireAt`. Use `gcloud firestore fields ttls update --collection-group=metrics --enable-ttl --field=expireAt --async` (or via Firebase console). Document in `docs/wearables/open-wearables-deploy.md`.

    - **Composite indexes** (`firestore.indexes.json` extended): add `{ collectionGroup: 'healthSamples', fields: [{fieldPath:'recordedAt', order:'DESCENDING'}, {fieldPath:'metric', order:'ASCENDING'}] }` (already in Plan 01 — verify).

    - **WEAR-12 anti-feature: NO automated medical alerts**:
        - Test `no-medical-alerts.test.ts` asserts that `aggregateDailyHealth` does NOT call any Cloud Function or write any doc that:
            - Sends notification with health-data-derived "alert" (e.g., "Your HR is dangerously low").
            - Triggers Discord DM with health-pattern flag.
            - Writes to `/users/{uid}/medicalAlerts` collection.
        - The test scans the source code via AST/grep for forbidden patterns: `medical_alert`, `health_alarm`, `cardiac`, `arrhythmia`, `clinical`. Expects 0 matches outside test files.

    - **ADR-011 fork strategy** (per WEAR-04 + RESEARCH Open Question 5):
        - Decision: At Phase 2 launch, fork `the-momentum/open-wearables` to `gamechangers/open-wearables` GitHub repo. Pin to v0.4.3 commit. Subscribe to upstream releases via Watch. Plan budget for fork-and-maintain in Phase 4.
        - Mitigations: (1) maintain visible fallback "manual entry" path (Plan 08 already delivers); (2) Document the deploy procedure in `docs/wearables/open-wearables-deploy.md` (Hetzner CX22 + Docker Compose + provider OAuth setup).

    - **Tests**:
        - `openWearablesWebhook.test.ts`:
            - Valid HMAC + valid payload + active consent → samples written, daily rollup task scheduled.
            - Invalid HMAC → 401, no DB write.
            - Active payload but consent revoked → 403, audit entry "wearable_sample_dropped_no_consent".
            - Replay (same sample IDs) → idempotent (no double-write).
        - `aggregateDailyHealth.test.ts`: seed 100 samples for one day → assert healthDaily doc has correct sum/count/avg.
        - `disconnectDevice.test.ts`: retainData=true → samples retained, _lookup deleted; retainData=false → samples deleted via Cloud Task.
        - `no-medical-alerts.test.ts`: AST/grep scan asserting absence of forbidden alert patterns.
        - `tests/rules/wearables-data-consent.test.ts`: extends Plan 01 rules tests — assert client write to /users/{uid}/healthSamples ALWAYS DENIED (only Functions service-account writes); client read requires `wearable_data` consent claim.
  </behavior>
  <action>
    1. Update `functions/wearables/package.json` with deps: `firebase-functions@7.2.5`, `firebase-admin@13.8.0`, `@google-cloud/tasks@^5.x`, `@gamechangers/functions-shared`, `@gamechangers/shared`, `zod@4.3.6`. devDep `firebase-functions-test`, `vitest`.

    2. Author `functions/wearables/src/openWearablesWebhook.ts` per <behavior>. Use `req.rawBody` (Functions v2 onRequest provides this). Read `OW_HMAC_SECRET` via `defineSecret`.

    3. Author `aggregateDailyHealth.ts` (Cloud Task HTTP target). Document at the top: this Function is invoked by Cloud Tasks scheduled in `openWearablesWebhook.ts` with 60s delay for debounce.

    4. Author `connectDevice.ts` and `disconnectDevice.ts` per <behavior>. Read `OPEN_WEARABLES_BASE_URL` via env.

    5. Author `functions/consent/src/wearableRevokeHandler.ts` subscribing to `consent-revoked` topic; on `category=='wearable_data'`, call `disconnectDevice` Function.

    6. Update `functions/wearables/src/index.ts` exports: openWearablesWebhook, aggregateDailyHealth, disconnectDevice, connectDevice. Add `import './sentry-init';`.

    7. Update `functions/consent/src/index.ts` exports adding `wearableRevokeHandler`.

    8. Author `docs/architecture/ADR-011-wearables-fork-strategy.md` per <behavior>.

    9. Author `docs/wearables/open-wearables-deploy.md` documenting:
        - Hetzner CX22 specs ($10/mo Falkenstein).
        - `git clone https://github.com/the-momentum/open-wearables && git checkout v0.4.3` then fork instructions.
        - `docker-compose up -d`.
        - Provider OAuth registration list with links (Garmin Connect IQ, Fitbit, Polar AccessLink, Whoop, Oura Cloud).
        - Webhook config: outgoing URL = `https://southamerica-east1-<project>.cloudfunctions.net/wearables-openWearablesWebhook`.
        - HMAC secret generation and storage in Open Wearables admin + GCP Secret Manager.

    10. Update `firestore.indexes.json` if not already present.

    11. Configure Firestore TTL policy via `gcloud firestore fields ttls update`. Document the exact command.

    12. Author 4 Function tests + 1 Rules test per <behavior>.

    13. Run `pnpm --filter @gamechangers/functions-wearables test`.
  </action>
  <verify>
    <automated>pnpm --filter @gamechangers/functions-wearables test && pnpm --filter @gamechangers/tests-rules test</automated>
  </verify>
  <acceptance_criteria>
    - File `functions/wearables/src/openWearablesWebhook.ts` calls `verifyHmacSha256` AND `consentGate(uid, 'wearable_data')` (grep both).
    - File `functions/wearables/src/openWearablesWebhook.ts` writes to monthly bucket path matching `users/${uid}/healthSamples/${yyyymm}/metrics/${id}` (grep regex).
    - File `functions/wearables/src/openWearablesWebhook.ts` schedules Cloud Task with 60s delay for daily rollup (grep `60` and `daily-health-rollup`).
    - File `functions/wearables/src/aggregateDailyHealth.ts` writes to `/users/{uid}/healthDaily/{yyyy-mm-dd}` (grep `healthDaily`).
    - File `functions/consent/src/wearableRevokeHandler.ts` subscribes to topic `consent-revoked` AND calls `disconnectDevice` only when category=='wearable_data'.
    - File `docs/architecture/ADR-011-wearables-fork-strategy.md` exists; states "fork at Phase 2 launch" and pins `v0.4.3`.
    - File `docs/wearables/open-wearables-deploy.md` exists; includes the webhook URL pattern AND provider list.
    - Test `no-medical-alerts.test.ts` asserts no occurrences of `medical_alert|health_alarm|cardiac|arrhythmia|clinical` in functions/wearables/ source files.
    - Test `wearables-data-consent.test.ts` exits 0 — proves client write DENIED + client read requires consent claim.
    - All Function + Rules tests exit 0.
  </acceptance_criteria>
  <done>Open Wearables webhook integrated: HMAC + consent + monthly bucket + debounced daily rollup; disconnect via user OR consent revoke cascade; TTL 90d on healthSamples; ADR-011 fork strategy documented; WEAR-12 anti-feature enforced by source-scan test; Apple HealthKit + Health Connect explicitly deferred per D-11.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BigQuery anonymized two-tier B2B pipeline — 5 audience segments with k≥50 + ε-DP + minor exclusion + Metabase service account scoping (raw access REVOKED)</name>
  <files>
    bigquery/views/active_movers_by_city.sql,
    bigquery/views/social_connectors_by_age_band.sql,
    bigquery/views/competitive_core_by_cluster.sql,
    bigquery/views/new_recruits.sql,
    bigquery/views/at_risk_segment.sql,
    bigquery/setup/create-datasets.sh,
    bigquery/setup/grant-metabase-readonly.sh,
    bigquery/setup/revoke-raw-access.sh,
    tests/bigquery/anonymized-views.test.sql,
    functions/b2b/src/index.ts,
    functions/b2b/src/partnerEmbedJwt.ts,
    functions/b2b/src/__tests__/partnerEmbedJwt.test.ts,
    functions/b2b/package.json
  </files>
  <read_first>
    .planning/phases/02-platform-mvp/02-RESEARCH.md (§3 Two-tier B2B pipeline IN FULL — Code Example 3 with k>=50 + WITH DIFFERENTIAL_PRIVACY OPTIONS; B2BD-01..14 from REQUIREMENTS.md as forward-reference for what Phase 3 needs from this scaffold),
    .planning/research/PITFALLS.md (Pitfall #6 retrofit cost),
    .planning/phases/02-platform-mvp/02-CONTEXT.md (D-14 minors NEVER in B2B; D-16 three b2b_* categories),
    docs/architecture/ADR-009-bigquery-export-scope.md (from Plan 01 — what's exported)
  </read_first>
  <behavior>
    - **`bigquery/setup/create-datasets.sh`**:
        - Create dataset `gw_b2b_views` in southamerica-east1 (or US, since BigQuery DP is GA in US/EU; document the choice — likely `US` for DP availability, with cross-border-transfer DPIA note).
        - **NOTE**: BigQuery DP availability varies by region. If southamerica-east1 doesn't yet have DP GA (verify at deploy time via `bq ls`), use `US` and document in ADR-009 update. The Firestore→BigQuery extension exports go to whichever region is configured in Plan 01.

    - **5 BigQuery anonymized SQL views** (per RESEARCH §3 Code Example 3 + B2BD-08 segment list):

        1. **`active_movers_by_city.sql`**:
           ```sql
           CREATE OR REPLACE VIEW `${PROJECT}.gw_b2b_views.active_movers_by_city` AS
           SELECT WITH DIFFERENTIAL_PRIVACY
             OPTIONS(
               epsilon = 1.0,
               delta = 1e-5,
               privacy_unit_column = user_id,
               max_groups_contributed = 5
             )
             p.city,
             COUNT(DISTINCT p.user_id) AS active_user_count,
             AVG(c.weekly_challenges) AS avg_challenges,
             AVG(h.steps_7d_avg) AS avg_steps_7d
           FROM `${PROJECT}.gw_analytics.profiles_changelog` p
           JOIN `${PROJECT}.gw_analytics.consents_changelog` cb USING (user_id)
           LEFT JOIN `${PROJECT}.gw_analytics.challenges_aggregate` c USING (user_id)
           LEFT JOIN `${PROJECT}.gw_analytics.healthDaily_changelog` h USING (user_id)
           WHERE cb.category = 'b2b_brands'
             AND cb.status = 'granted'
             AND cb.expires_at > CURRENT_TIMESTAMP()
             AND p.is_minor = FALSE
             AND p._change_type != 'DELETE'
             AND c.weekly_challenges >= 3
           GROUP BY p.city
           HAVING COUNT(DISTINCT p.user_id) >= 50;
           ```

        2. **`social_connectors_by_age_band.sql`**: 2+ events/month, joined on age_band 5-year bucket, grouped, k≥50.

        3. **`competitive_core_by_cluster.sql`**: high engagement on cluster-specific challenges, grouped by gameCluster, k≥50.

        4. **`new_recruits.sql`**: users joined within last 30 days, grouped by city + age_band, k≥50.

        5. **`at_risk_segment.sql`**: users with declining attendance (last 30d events < prior 30d events by 50%), grouped at city level, k≥50. **CRITICAL**: this segment is sensitive — RESEARCH `at_risk` is a B2B notion of churn, NOT a clinical health notion (per WEAR-12 anti-feature). The view filters `health_self_reports` consent + `is_minor=FALSE`.

    - All views share these defenses:
        - `WITH DIFFERENTIAL_PRIVACY OPTIONS(epsilon=1.0, delta=1e-5, privacy_unit_column=user_id, max_groups_contributed=5)`.
        - `HAVING COUNT(DISTINCT user_id) >= 50` (k-anonymity floor).
        - `WHERE is_minor = FALSE` (D-14 minor exclusion).
        - `WHERE _change_type != 'DELETE'` (post-erasure exclusion per Plan 04).
        - Per-partner consent JOIN: only users granting the relevant `b2b_*` category.

    - **`bigquery/setup/grant-metabase-readonly.sh`**:
        ```bash
        # Create service account
        gcloud iam service-accounts create metabase-readonly --display-name="Metabase B2B readonly"
        # Grant SELECT on gw_b2b_views ONLY
        bq update --view_definition_field --grant_role 'roles/bigquery.dataViewer' \
          --member 'serviceAccount:metabase-readonly@${PROJECT}.iam.gserviceaccount.com' \
          ${PROJECT}:gw_b2b_views
        # Grant bq job user (to run the views)
        gcloud projects add-iam-policy-binding ${PROJECT} \
          --member 'serviceAccount:metabase-readonly@${PROJECT}.iam.gserviceaccount.com' \
          --role 'roles/bigquery.jobUser'
        ```

    - **`bigquery/setup/revoke-raw-access.sh`**:
        ```bash
        # CRITICAL: revoke any access metabase-readonly may have on gw_analytics
        bq update --remove_role 'roles/bigquery.dataViewer' \
          --member 'serviceAccount:metabase-readonly@${PROJECT}.iam.gserviceaccount.com' \
          ${PROJECT}:gw_analytics
        # Belt-and-braces: assert via gcloud
        gcloud projects get-iam-policy ${PROJECT} \
          --flatten='bindings[].members' \
          --filter='bindings.members:metabase-readonly' \
          --format='value(bindings.role)' \
          | grep -v 'gw_b2b_views\|jobUser' \
          && { echo "ERROR: metabase-readonly has unexpected role"; exit 1; }
        echo "OK: metabase-readonly has only gw_b2b_views access + jobUser"
        ```

    - **`tests/bigquery/anonymized-views.test.sql`** (run via `bq query --use_legacy_sql=false`):
        - Test 1: `SELECT * FROM gw_b2b_views.active_movers_by_city` returns rows ONLY where active_user_count >= 50.
        - Test 2: Try `SELECT * FROM gw_analytics.profiles_changelog` as the metabase-readonly service account — MUST fail with permission-denied.
        - Test 3: Every view has `is_minor = FALSE` clause (`bq query 'SELECT view_definition FROM gw_b2b_views.INFORMATION_SCHEMA.VIEWS'` — assert each definition includes `is_minor = FALSE`).

    - **`functions/b2b/src/partnerEmbedJwt.ts`**: scaffolding for Phase 3 Metabase iframe embed. Generates a signed JWT with partner_id claim that Metabase verifies for per-partner row filters. **In Phase 2, this Function exists but only allows authenticated users with `role: 'admin'` to test-issue tokens; no real partner is connected.** Phase 3 will gate via partner consent verification.

    - Test `partnerEmbedJwt.test.ts`: assert JWT issuance with admin claim works; non-admin → permission-denied.
  </behavior>
  <action>
    1. Author 5 SQL view files in `bigquery/views/`. Use `${PROJECT}` placeholder; setup script substitutes via `envsubst`.

    2. Author 3 setup bash scripts. Each is idempotent (`|| true` on creates; `--no-error-on-existing` flags where supported).

    3. Author `tests/bigquery/anonymized-views.test.sql` per <behavior>. Document how to run: `bq query --use_legacy_sql=false < tests/bigquery/anonymized-views.test.sql` invoked from a CI workflow `bigquery-views-audit.yml` (similar pattern to TOS audit in Plan 03 — runs quarterly OR on changes to bigquery/views/).

    4. Author `functions/b2b/src/partnerEmbedJwt.ts` + test.

    5. Update `functions/b2b/src/index.ts` exports.

    6. Add CI workflow `.github/workflows/bigquery-views-audit.yml`:
        - Cron `0 0 1 */3 *` (quarterly).
        - Runs `bash bigquery/setup/revoke-raw-access.sh` (idempotent — if metabase-readonly somehow gained gw_analytics access, this revokes it AND fails the workflow).
        - Runs the SQL test asserting all 5 views have HAVING k>=50 + is_minor=FALSE.
        - Notifies srparca@gmail.com on failure.

    7. Run `pnpm --filter @gamechangers/functions-b2b test`.

    8. Manual: trigger the bigquery-views-audit workflow once and verify it passes.
  </action>
  <verify>
    <automated>pnpm --filter @gamechangers/functions-b2b test && grep -c "HAVING COUNT(DISTINCT user_id) >= 50" bigquery/views/*.sql</automated>
  </verify>
  <acceptance_criteria>
    - 5 SQL view files exist in `bigquery/views/` with exact names listed.
    - Every view file contains the EXACT string `HAVING COUNT(DISTINCT user_id) >= 50` (grep returns 5+ matches).
    - Every view file contains the EXACT string `is_minor = FALSE` (D-14 enforcement; grep returns 5+ matches).
    - Every view file contains `WITH DIFFERENTIAL_PRIVACY OPTIONS(` AND `epsilon = 1.0`.
    - File `bigquery/setup/revoke-raw-access.sh` contains `metabase-readonly` AND `gw_analytics` AND removes role (grep `--remove_role`).
    - File `tests/bigquery/anonymized-views.test.sql` contains a test asserting metabase-readonly cannot SELECT from gw_analytics.
    - File `.github/workflows/bigquery-views-audit.yml` exists with quarterly cron AND runs revoke-raw-access.sh.
    - Function `partnerEmbedJwt` requires admin claim (grep `role.*admin` in source).
    - All Function tests + grep counts succeed.
  </acceptance_criteria>
  <done>5 BigQuery anonymized B2B views with k≥50 + ε-DP=1.0 + is_minor=FALSE + post-erasure exclusion; Metabase service account has SELECT on gw_b2b_views ONLY (raw access revoked); quarterly audit CI workflow gates regressions; partnerEmbedJwt scaffolded for Phase 3 Metabase iframes; per Pitfall #6 the entire two-tier pipeline is built and tested in Phase 2 with no partner connected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PWA wearables surface — connect/disconnect UI, provider cards, character sheet integration via Layer 3 consent</name>
  <files>
    apps/pwa/src/views/wearables/Wearables.vue,
    apps/pwa/src/views/wearables/ConnectDevice.vue,
    apps/pwa/src/components/WearableProviderCard.vue,
    apps/pwa/src/composables/useWearables.ts,
    apps/pwa/src/router/index.ts,
    apps/pwa/src/locales/es.json,
    apps/pwa/src/locales/en.json,
    apps/pwa/src/__tests__/useWearables.test.ts
  </files>
  <read_first>
    .planning/phases/02-platform-mvp/02-UI-SPEC.md (§Copywriting wearable connect CTAs + disconnect modal),
    apps/pwa/src/composables/useConsent.ts (from Plan 04 — wearable_data is Layer 3),
    apps/pwa/src/views/me/CharacterSheet.vue (from Plan 05 — consumes healthDaily once connected),
    functions/wearables/src/connectDevice.ts (from Task 1)
  </read_first>
  <behavior>
    - **`Wearables.vue`** (route `/me/wearables`):
        - If `!hasGranted('wearable_data')`: show banner `Activá Layer 3 para conectar tu pulsera` linking to `/consent/layer-3`.
        - Else: lists connected devices (subscribed to `/users/{uid}/_lookup/wearable/*`). Each shows provider, last sync, "Desconectar" button.
        - "Conectar dispositivo" CTA → `/me/wearables/connect`.
        - Empty state per UI-SPEC §Empty states: `Aún sin dispositivo conectado` + body `Tu teléfono ya cuenta tus pasos. Si quieres, conecta una pulsera para ver más detalle — totalmente opcional.` (CHLG-04 + Pitfall #9 reinforcement).

    - **`ConnectDevice.vue`** (route `/me/wearables/connect`): grid of WearableProviderCard for each of 5 supported providers (Garmin / Fitbit / Polar / Whoop / Oura). Each card shows provider logo + name + "Conectar" CTA → calls `connectDevice` callable → opens returned oauthUrl in new tab.

    - **`WearableProviderCard.vue`**: provider logo, name, brief description, "Conectar" or "Conectado" indicator with last-sync timestamp.

    - **`useWearables.ts`**: composable exposing `connectedDevices`, `connect(provider)`, `disconnect(provider, retainData)`, `isConnected(provider)`.

    - **Disconnect modal** (uses UI-SPEC destructive confirmations pattern):
        - Title: `Desconectar {provider}`
        - Body: `Dejaremos de recibir datos. Eligí qué hacer con lo ya guardado:`
        - Radio options: `Conservarlo` (default) / `Borrarlo todo`.
        - CTA: `Desconectar` (destructive button).

    - **Character sheet integration**: Plan 05's CharacterSheet.vue ALREADY shows full color even without wearable. Once Layer 3 is granted AND a device connected, the StatRow trend indicator activates (computed from healthDaily 7d trend). Plan 05 already subscribed to profile/main.stats, which `recomputeStats` (Plan 05 + extended here) updates to incorporate healthDaily.

    - **`recomputeStats` extension** (in Plan 05 file `functions/gamification/src/recomputeStats.ts` — additive change):
        - Read `/users/{uid}/healthDaily/*` last 7 days when computing HP. Extension contributes additional HP increments.
        - Document this Plan-05-additive change in this Plan's SUMMARY.

    - **i18n keys ADDED**:
        - `wearables.heading`: `Tus dispositivos`
        - `wearables.empty.heading`: `Aún sin dispositivo conectado`
        - `wearables.empty.body`: `Tu teléfono ya cuenta tus pasos. Si quieres, conecta una pulsera para ver más detalle — totalmente opcional.`
        - `wearables.cta.connect`: `Conectar dispositivo` (already exists)
        - `wearables.consent_required`: `Activá Layer 3 (datos de pulsera) para conectar tu dispositivo.`
        - `wearables.disconnect.modal.title_template`: `Desconectar {provider}`
        - `wearables.disconnect.modal.body`: `Dejaremos de recibir datos. Eligí qué hacer con lo ya guardado:`
        - `wearables.disconnect.option.retain`: `Conservarlo`
        - `wearables.disconnect.option.delete`: `Borrarlo todo`
        - `wearables.disconnect.cta`: `Desconectar`
        - `wearables.last_sync_template`: `Última sincronización: {timestamp}`
        - `wearables.providers.garmin`: `Garmin`
        - `wearables.providers.fitbit`: `Fitbit`
        - `wearables.providers.polar`: `Polar`
        - `wearables.providers.whoop`: `Whoop`
        - `wearables.providers.oura`: `Oura`
        - `wearables.error.oauth_failed`: `No conectamos tu dispositivo`
        - `wearables.error.oauth_failed_body`: `Probá de nuevo desde la app de tu pulsera, o seguí registrando manualmente — funciona igual.`

    - **Test**: `useWearables.test.ts`:
        - Without Layer 3 consent: `connect()` throws (or no-op with toast); `connectedDevices` remains empty.
        - With Layer 3 consent: `connect('garmin')` calls callable + receives oauthUrl.
        - `disconnect('garmin', false)` calls callable with retainData=false.
  </behavior>
  <action>
    1. Author the 4 PWA Vue files per <behavior>.

    2. Author `useWearables.ts` composable.

    3. Update `apps/pwa/src/router/index.ts` adding 2 routes: `/me/wearables`, `/me/wearables/connect`.

    4. Add i18n keys per <behavior>.

    5. Update `functions/gamification/src/recomputeStats.ts` (Plan 05 file — additive change) to read `/users/{uid}/healthDaily/*` last 7 days when computing HP. The Pitfall #9 floor of 1 still applies; healthDaily contributes ADDITIVE points.

    6. Author `useWearables.test.ts` per <behavior>.

    7. Run `pnpm vitest run apps/pwa/src/__tests__/useWearables.test.ts`.
  </action>
  <verify>
    <automated>pnpm vitest run apps/pwa/src/__tests__/useWearables.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/pwa/src/views/wearables/Wearables.vue` checks `hasGranted('wearable_data')` and renders Layer 3 consent CTA when missing (grep `wearable_data` AND `consent`).
    - File `apps/pwa/src/views/wearables/Wearables.vue` empty state copy matches UI-SPEC: "Aún sin dispositivo conectado" + "Tu teléfono ya cuenta tus pasos" (grep both).
    - File `apps/pwa/src/components/WearableProviderCard.vue` lists 5 providers (grep all 5 names).
    - File `apps/pwa/src/views/wearables/ConnectDevice.vue` calls `connectDevice` callable (grep `connectDevice` AND `httpsCallable`).
    - File `functions/gamification/src/recomputeStats.ts` reads from healthDaily (grep `healthDaily`); preserves Pitfall #9 floor of 1.
    - Test `useWearables.test.ts` exits 0 with the consent gate + connect/disconnect cases.
  </acceptance_criteria>
  <done>Wearables PWA surface complete: connect/disconnect UI, 5 providers, Layer 3 consent gate, retain/delete option on disconnect; recomputeStats extended to incorporate healthDaily without losing Pitfall #9 floor.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <files>(none — verification checkpoint)</files>
  <action>This is a human-verify checkpoint. The Claude executor pauses here and presents the <how-to-verify> steps below to the user. The user manually completes each verification, replies with the resume-signal, then the executor continues. No code changes happen in this task.</action>
  <verify>Manual — see <how-to-verify> below.</verify>
  <done>User replies with the resume-signal confirming all verification items pass. If any item fails, file follow-up tasks before marking Phase 2 complete.</done>
  <name>Task 4: Phase 2 acceptance gate — DPO sign-off, Open Wearables deploy verification, end-to-end 90-second journey check</name>
  <what-built>
    Phase 2 is now complete. This checkpoint verifies the platform success gate from ROADMAP.md:
    - Discord member can flow `/link` → app account → granted Layer 1 consent → first event QR check-in with XP awarded in under 90 seconds, conversion measured in PostHog
    - Granular consent UI with 10 individually-revocable categories, plain Spanish (LATAM "tú"), versioned consent texts, hash-chained audit ledger, DSAR within 30 days, account erasure within 72 hours
    - User without wearable can view full-color character sheet, join Bronce/Plata/Oro challenge, log progress manually or via phone pedometer or photo, see opt-in leaderboard, earn badges
    - Organizer can run meetup with 50+ attendees on poor venue connectivity using offline QR check-in (IDB queue + Background Sync), one-tap report-user, post-event recap card auto-generates
    - Two-tier B2B data architecture: firestore-raw DPO-only → bigquery-anonymized with k≥50 + ε-DP deployed and tested even though no B2B partner connected yet
  </what-built>
  <how-to-verify>
    1. **DPO + counsel sign-off on ADR-010 (audit-retention vs erasure reconciliation from Plan 04)** — BLOCKER for production deploy. Confirm signed off in writing or note as outstanding.

    2. **Open Wearables deployment verification**:
       - Visit the FastAPI hub URL; confirm running v0.4.3.
       - Trigger a test sample via OW admin → verify webhook receipt in `/users/{testUid}/healthSamples/{yyyymm}/metrics/`.
       - Verify daily rollup at `/users/{testUid}/healthDaily/{date}` after 60s.
       - Verify TTL policy active on `metrics` collection-group (`gcloud firestore fields ttls describe`).

    3. **End-to-end 90-second journey**:
       - From a Discord channel with the bot online, run `/link`. Receive a URL.
       - Open the URL in a browser. Complete OAuth → land on `/auth/discord/callback` → AgeGate (use 25-year-old birthDate) → Layer 0 grant (basic_profile).
       - Navigate to `/events`, click on a seeded event, click "Confirmar asistencia" → Layer 1 prompt → grant event_participation.
       - QR PNG renders. Open in another tab; navigate to `/events/:id/checkin` (organizer view); use webcam to scan the QR (or paste payload via DevTools).
       - Verify XP awarded (+100), badge `event:meetup-test:primera-vez` granted, profile updated.
       - Stopwatch this flow: must complete in <90 seconds.

    4. **WCAG 2.1 AA**: run `pnpm playwright test tests/e2e/a11y-events.spec.ts` (Plan 07) → 0 violations.

    5. **B2B SQL verification** (run as `metabase-readonly@<project>.iam.gserviceaccount.com` via `bq query`):
       - `SELECT * FROM gw_b2b_views.active_movers_by_city` succeeds.
       - `SELECT * FROM gw_analytics.profiles_changelog` MUST fail with permission-denied.

    6. **Quarterly audits configured**:
       - `gh workflow list` shows `tos-compliance-audit.yml` (Plan 03) AND `bigquery-views-audit.yml` (Plan 09 Task 2).
       - Manually trigger each via `gh workflow run` → both pass.

    7. **All 109 phase requirement IDs delivered**: cross-check `ARCH-01..10, AUTH-01..12, CNST-01..14, PROF-01..14, EVNT-01..14, CHLG-01..12, WEAR-01..12, CONT-01..07, DBOT-01..07, A11Y-01..09` against the SUMMARY files of Plans 01–09.

    8. **Kill criterion** (per ROADMAP Success Criterion #1): If PostHog funnel `discord_link_callback → consent_layer_0_granted` shows <60% conversion in first cohort of testers, document and **STOP** before broad rollout (this is a Phase 2 → Phase 3 gate).
  </how-to-verify>
  <resume-signal>
    Reply `approved` to mark Phase 2 complete, OR describe blocking issues. Specifically confirm:
    - [ ] DPO + counsel signed off on ADR-010
    - [ ] Open Wearables deployed and webhook flowing
    - [ ] 90-second journey completed in target time
    - [ ] WCAG 2.1 AA Playwright spec exits 0
    - [ ] BigQuery permission boundary confirmed via SQL
    - [ ] Quarterly audit workflows configured and passing
    - [ ] All 109 requirement IDs traceable to a Plan
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Open Wearables FastAPI → our Function | HMAC + timing-safe verification; consent gate; 90d TTL minimization |
| Cloud Functions → Cloud Tasks → aggregateDailyHealth | Cloud Tasks queue with rate limit; signed via Functions service account |
| BigQuery `gw_analytics` | DPO-only; Metabase service account REVOKED |
| BigQuery `gw_b2b_views` | k≥50 + ε-DP + minor exclusion + post-erasure exclusion |
| consent revoke → wearable disconnect | Pub/Sub topic consumed by wearableRevokeHandler in functions/consent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09-01 | Spoofing | Forged webhook from non-Open-Wearables source | HIGH | mitigate | Task 1: HMAC verification with timing-safe comparison; OW_HMAC_SECRET in Secret Manager |
| T-02-09-02 | Tampering | Sample replay (same sample.id) | MEDIUM | mitigate | Task 1: deterministic doc ID `metrics/{s.id}`; merge:true write idempotent |
| T-02-09-03 | Information Disclosure | Raw healthSamples leak via BigQuery | HIGH | mitigate | Plan 01 ADR-009 + Task 1: BQ export config explicitly excludes healthSamples; only healthDaily exported; Task 2 view tests assert raw access revoked |
| T-02-09-04 | Information Disclosure | k=1 re-identification on small cohort | HIGH | mitigate | Task 2: k≥50 SQL HAVING + ε-DP epsilon=1.0; minor exclusion; region collapse for small cohorts (handled at write time in profiles_changelog generalization — added in Plan 04 if not yet) |
| T-02-09-05 | Information Disclosure | Minor data B2B leak | HIGH | mitigate | Task 2: every view filters `is_minor = FALSE`; D-14 enforced at SQL layer |
| T-02-09-06 | Compliance | Post-erasure data persists in BQ | HIGH | mitigate | Task 2: every view filters `_change_type != 'DELETE'` (Firestore→BQ extension marks deletions); Plan 04 erasure cascades via deleted_at flag |
| T-02-09-07 | Repudiation | Health-derived medical alert sent (anti-feature WEAR-12) | HIGH | mitigate | Task 1: no-medical-alerts.test.ts source-scan; aggregateDailyHealth contains no notification triggers; documented anti-feature |
| T-02-09-08 | DoS | Garmin sync burst (2K samples in 5s) overwhelms aggregateDailyHealth | MEDIUM | mitigate | Task 1: Cloud Tasks queue `daily-health-rollup` rate-limit 100 RPS + 60s debounce delay |
| T-02-09-09 | Information Disclosure | Open Wearables JWT leak | MEDIUM | mitigate | docs/wearables/open-wearables-deploy.md: secrets in Secret Manager / VPS env; not in repo; rotate quarterly |
| T-02-09-10 | Compliance | Cross-border data transfer not in DPIA | HIGH | mitigate | Task 2: BigQuery US region (DP availability) requires DPIA cross-border note; Plan 04 cross_border consent category covers this; document in ADR-009 update |
| T-02-09-11 | Tampering | Metabase service account silently regains gw_analytics access | HIGH | mitigate | Task 2: revoke-raw-access.sh + quarterly bigquery-views-audit.yml CI cron asserting + reverting |
| T-02-09-12 | Information Disclosure | Wearable data persists past 90 days violating LOPDP minimization | MEDIUM | mitigate | Task 1: Firestore TTL policy on metrics collection-group with expireAt field |
| T-02-09-13 | Tampering | Consent revoke doesn't propagate to OW server | HIGH | mitigate | Task 1: wearableRevokeHandler subscribes to consent-revoked topic, calls disconnectDevice → POSTs OW disconnect |
</threat_model>

<verification>
- All WEAR-01..12 reachable: HealthKit/Health Connect deferred per D-11 (WEAR-01/02 marked as Phase 3 in ADR); Open Wearables webhook (WEAR-03); fork strategy ADR-011 (WEAR-04); HMAC + consent (WEAR-05); normalized schema (WEAR-06); monthly buckets (WEAR-07); daily rollup (WEAR-08); BQ export only daily (WEAR-09); disconnect with retain/delete (WEAR-10); auto-tracking but NEVER required (WEAR-11 — Plan 08 manual-first proves this); no medical alerts (WEAR-12 + assertion test).
- Two-tier B2B pipeline complete with k≥50 + ε-DP + minor exclusion verified by SQL test.
- Phase 2 acceptance checkpoint enumerates the 90-second journey, DPO sign-off, audit workflow status.
</verification>

<success_criteria>
- 5 BigQuery views exist with k≥50 + ε-DP + is_minor=FALSE; raw access revoked from Metabase service account; quarterly audit gates regressions.
- Open Wearables webhook receives + persists samples; 90-day TTL active; daily rollup runs.
- User can connect Garmin/Fitbit/Polar/Whoop/Oura; disconnect with retain/delete option.
- WEAR-12 anti-feature enforced by source-scan test.
- ADR-011 documents fork strategy; deploy guide complete.
- Phase 2 success gate met (or kill criterion documented if not).
</success_criteria>

<output>
After completion, create `.planning/phases/02-platform-mvp/02-09-SUMMARY.md` documenting:
- Open Wearables webhook URL + HMAC secret rotation cadence
- Time-series schema (monthly buckets) + daily rollup debounce
- TTL policy (90d on healthSamples)
- BigQuery dataset names: `gw_analytics` (raw, DPO-only) + `gw_b2b_views` (anonymized)
- 5 audience segment views and their filters
- IAM scoping: metabase-readonly has SELECT on gw_b2b_views ONLY
- Apple HealthKit / Health Connect explicitly DEFERRED per D-11
- Plan 05 recomputeStats additive extension reading healthDaily
- ADR-011 fork strategy reference
- Phase 2 final delivery summary: 109/109 requirements delivered
</output>
