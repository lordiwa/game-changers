---
phase: 02-platform-mvp
plan: 16
type: execute
wave: 8
depends_on: [02-11, 02-15]
gap_closure: true
files_modified:
  - docs/operations/open-wearables-hetzner-deploy.md
  - docs/wearables/open-wearables-deploy.md
autonomous: false
requirements:
  - WEAR-03
  - WEAR-05
  - WEAR-06
  - WEAR-07
gap_closure_targets:
  - G8

# Revision history:
# - rev-1 (2026-05-09): Fixed Major-6 (test-user cleanup was "optional" — too soft for
#   production health data + LOPDP active-deletion duty). Cleanup is now MANDATORY: run
#   account erasure (via Cloud Tasks per 02-15) on the test uid, document BigQuery
#   `_changelog` purge timeline + DSAR fallback. Added depends_on: [02-11, 02-15] —
#   needs 02-11 (BigQuery export pipeline so we know whether the test sample reached
#   gw_analytics) and 02-15 (Cloud Tasks erasure infrastructure for the cleanup itself).

must_haves:
  truths:
    - "Hetzner CX22 VPS provisioned in Falkenstein (FSN1) running Ubuntu 22.04 with Docker + Docker Compose"
    - "Open Wearables 0.4.3 deployed via the official `the-momentum/open-wearables` Docker Compose stack and reachable on a stable HTTPS URL (e.g., `https://ow.gamechangers.gg`) with a valid TLS cert (Let's Encrypt or equivalent)"
    - "Outgoing webhook on the Open Wearables hub points at `https://southamerica-east1-gamechangers-prod.cloudfunctions.net/wearables-openWearablesWebhook`"
    - "`OW_HMAC_SECRET` placeholder in Secret Manager is replaced with the real HMAC secret shared with the Open Wearables admin AND the Cloud Function reads the same value (proven by a test webhook delivery)"
    - "End-to-end: a real Garmin (or Fitbit/Polar/Whoop/Oura) sample sent through Open Wearables lands in `/users/{testUid}/healthSamples/{yyyy-mm}/metrics/{id}` with `source: 'open-wearables'` and the daily rollup at `/users/{testUid}/healthDaily/{yyyy-mm-dd}` is computed within 60s"
    - "MANDATORY cleanup post-smoke-test: test user account is erased via Cloud Tasks-backed accountErasure (per 02-15) — NOT optional; LOPDP requires active deletion of test data with health-data scope. BigQuery `_changelog` purge timeline + DSAR fallback documented."
    - "PARTIAL OK fallback documented: if VPS budget is not yet approved, this plan exits with a documented `BLOCKED-DEFERRED` state — the code-side webhook handler is already production-ready, so the platform functions on self-reports + phone pedometer until the hub is provisioned"
  artifacts:
    - path: "docs/operations/open-wearables-hetzner-deploy.md"
      provides: "Operator runbook + run-log for Hetzner CX22 deploy + Open Wearables 0.4.3 install + provider OAuth registration + mandatory test-user cleanup procedure"
    - path: "docs/wearables/open-wearables-deploy.md"
      provides: "Existing high-level deploy guide from Plan 09 — this plan EXTENDS it with the actual production-deploy artifact (URL, secret rotation cadence, webhook smoke-test record)"
  key_links:
    - from: "Open Wearables FastAPI hub on Hetzner CX22"
      to: "wearables-openWearablesWebhook Function in southamerica-east1"
      via: "Outgoing webhook with HMAC-SHA256 over raw body"
      pattern: "X-OW-Signature"
    - from: "OW_HMAC_SECRET (Secret Manager)"
      to: "Open Wearables admin secret + Cloud Function shared secret"
      via: "Same value in two locations; rotated quarterly"
      pattern: "OW_HMAC_SECRET"
    - from: "Test user smoke-test data"
      to: "accountErasure Cloud Task -> erasureHardDelete (72h SLA)"
      via: "Plan 02-15 Cloud Tasks queue `erasure-hard-delete`"
      pattern: "Mandatory cleanup"
---

<objective>
Provision the Hetzner CX22 VPS, deploy Open Wearables 0.4.3, sync `OW_HMAC_SECRET` between the OW admin and Secret Manager (replacing the placeholder set by quick-task 260509-nmk), point the outgoing webhook at the live Cloud Function URL, and prove end-to-end delivery of one real wearable sample. The Cloud Function side (`wearables-openWearablesWebhook`) is already deployed and waiting per Plan 09; this plan delivers the missing infrastructure leg.

Purpose: Closes G8 (MEDIUM, blocked-deferred) from `02-HUMAN-UAT.md`. T3 acceptance test in 02-VERIFICATION.md is satisfied by this plan. Wearable data is the input that feeds character-sheet stat enrichment + challenge auto-tracking; without it, those features still function (manual-first per Pitfall #9 + Plan 08) but Layer 3 consent has nothing to deliver.

This plan declares `depends_on: [02-11, 02-15]`:
- **02-11** — Plan 02-11 brings the Firestore→BigQuery extension pipeline online. Without it, we don't know whether a smoke-test `healthSamples` doc would have been mirrored to BigQuery `gw_analytics` (the Major-7 invariant says healthSamples is NEVER exported, but a future operator drift could have changed this — 02-11 verifies the invariant; 02-16 relies on it).
- **02-15** — the mandatory cleanup of the test user (Major-6 fix) routes through the Cloud Tasks-backed accountErasure path established in 02-15. If 02-15 hasn't run, the cleanup falls back to the deprecated sweeper-doc and creates ambiguous paper-trails.

**Deferred-friendly:** if VPS budget ($10/mo Hetzner CX22) is not approved at execution time, the plan exits with a documented `BLOCKED-DEFERRED` state. The code-side webhook is already production-ready; nothing in the platform breaks. The plan can be re-run once the VPS budget is approved.

Output: Open Wearables hub reachable at a stable HTTPS URL; a real wearable sample lands in `/users/{testUid}/healthSamples/...`; `OW_HMAC_SECRET` rotated to real value; runbook documents the deploy AND the mandatory test-user cleanup procedure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-platform-mvp/02-HUMAN-UAT.md
@.planning/phases/02-platform-mvp/02-VERIFICATION.md
@.planning/phases/02-platform-mvp/02-09-PLAN.md
@.planning/phases/02-platform-mvp/02-09-SUMMARY.md
@.planning/phases/02-platform-mvp/02-11-PLAN.md
@.planning/phases/02-platform-mvp/02-15-PLAN.md
@CLAUDE.md
@docs/wearables/open-wearables-deploy.md
@docs/architecture/ADR-011-wearables-fork-strategy.md
@functions/wearables/src/openWearablesWebhook.ts

<interfaces>
```
# Open Wearables 0.4.3:
#   - GitHub: https://github.com/the-momentum/open-wearables (MIT, FastAPI/Python 3.11+)
#   - Pin to commit at v0.4.3 tag
#   - Components: FastAPI server, PostgreSQL 15, Redis (per CLAUDE.md Stack Patterns)
#
# Hetzner CX22 (target VPS):
#   - 2 vCPU, 4 GB RAM, 40 GB disk, ~$10/mo Falkenstein (FSN1) location
#   - Ubuntu 22.04 LTS image
#   - Public IPv4 + IPv6
#
# Webhook endpoint to configure in OW outgoing webhook:
https://southamerica-east1-gamechangers-prod.cloudfunctions.net/wearables-openWearablesWebhook
#
# Provider OAuth apps to register (per Plan 09 user_setup):
#   Garmin Connect IQ, Fitbit, Polar AccessLink, Whoop, Oura Cloud
#
# CRITICAL: ALL firebase CLI invocations use `pnpm exec firebase ...` (deploy-firebase-binary memory).
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decision gate — VPS budget approved?</name>
  <decision>Has the $10/mo Hetzner CX22 VPS budget been approved by the user?</decision>
  <context>
    Plan 02-16 closes G8 (Open Wearables hub). The platform code is production-ready (Plan 09 deployed `wearables-openWearablesWebhook`); only the FastAPI hub on a VPS is missing. Without the hub, Layer 3 wearable consent has no data source — but the rest of Phase 2 functions normally (manual-first per Pitfall #9). This decision determines whether we proceed with provisioning or mark G8 as `BLOCKED-DEFERRED`.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed: provision Hetzner CX22 + deploy Open Wearables 0.4.3</name>
      <pros>G8 closed end-to-end; T3 acceptance test passes; Layer 3 consent has data; ~$10/mo recurring; one-time setup ~2 hours.</pros>
      <cons>$10/mo recurring spend; ~2h provisioning; provider OAuth app registrations (5 portals) require manual work; pre-1.0 OW maintenance risk per ADR-011.</cons>
    </option>
    <option id="defer">
      <name>Defer: mark G8 as BLOCKED-DEFERRED + skip remaining tasks</name>
      <pros>No spend; no manual provider-portal work; platform still functions on self-reports + phone pedometer; T3 marked as `blocked` until budget approved (already its current state).</pros>
      <cons>Layer 3 consent has nothing to deliver until provisioning happens; Phase 2 acceptance gate "wearables hub deployed" remains open.</cons>
    </option>
  </options>
  <resume-signal>
    Reply `proceed` to continue with Task 2+, OR `defer` to skip Tasks 2-3 and write a `BLOCKED-DEFERRED` note in the SUMMARY.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Provision Hetzner CX22 + install Open Wearables 0.4.3 + obtain TLS cert</name>
  <files>
    docs/operations/open-wearables-hetzner-deploy.md,
    docs/wearables/open-wearables-deploy.md
  </files>
  <read_first>
    docs/wearables/open-wearables-deploy.md (Plan 09 deploy guide — read in full),
    docs/architecture/ADR-011-wearables-fork-strategy.md (Plan 09 fork strategy — pin v0.4.3, fork to gamechangers org)
  </read_first>
  <action>
    1. **Provision VPS** (operator step in Hetzner Console or via `hcloud` CLI):
       - Server type: CX22 (2 vCPU, 4 GB)
       - Location: FSN1 (Falkenstein)
       - Image: Ubuntu 22.04 LTS
       - SSH key: add the operator's public key
       - Name: `gamechangers-ow-prod`
       - Capture the public IPv4 + IPv6 to runbook Appendix A.

    2. **DNS**: assuming the operator owns `gamechangers.gg`, point `ow.gamechangers.gg` A record at the VPS IPv4. (If a different domain is used, document it.) Capture A record TTL.

    3. **OS hardening** (SSH into VPS):
       ```
       sudo apt update && sudo apt upgrade -y
       sudo apt install -y docker.io docker-compose-plugin caddy ufw
       sudo systemctl enable --now docker
       sudo usermod -aG docker $USER  # log out + back in
       sudo ufw allow 22 && sudo ufw allow 80 && sudo ufw allow 443 && sudo ufw enable
       ```

    4. **Fork** `the-momentum/open-wearables` to a `gamechangers/open-wearables` GitHub org (or to the operator's personal namespace) per ADR-011, then `git clone <fork-url>` on the VPS. Checkout `v0.4.3` tag explicitly (NOT `main` — pin to a tested version).

    5. Install dependencies + run via Docker Compose (per OW 0.4.3 README):
       ```
       cp .env.example .env
       # Edit .env: set HMAC_SECRET (generate with `openssl rand -hex 32`),
       #            POSTGRES_PASSWORD, REDIS_PASSWORD, BASE_URL=https://ow.gamechangers.gg
       docker compose up -d
       ```
       Capture the `HMAC_SECRET` value securely — it must be synced to GCP Secret Manager in Task 3.

    6. **TLS via Caddy** (simpler than nginx + certbot for a single host):
       ```
       sudo nano /etc/caddy/Caddyfile
       # Contents:
       #   ow.gamechangers.gg {
       #     reverse_proxy localhost:8000
       #   }
       sudo systemctl reload caddy
       ```
       Caddy auto-fetches Let's Encrypt cert on first request to the domain. Verify: `curl -I https://ow.gamechangers.gg/health` returns 200 (or whichever health endpoint OW exposes).

    7. **Provider OAuth apps** (operator step — repeat per provider):
       - Garmin Connect IQ developer portal -> register app -> callback `https://ow.gamechangers.gg/oauth/garmin/callback` -> record `CLIENT_ID` + `CLIENT_SECRET`.
       - Fitbit, Polar AccessLink, Whoop, Oura Cloud — same pattern with each provider's portal.
       - Add each `<PROVIDER>_CLIENT_ID` + `<PROVIDER>_CLIENT_SECRET` to OW `.env`; restart `docker compose up -d`.
       - Capture which providers were registered to runbook Appendix B (do NOT paste secrets — reference `.env` line numbers + Hetzner host).

    8. **Outgoing webhook config** in OW admin:
       - URL: `https://southamerica-east1-gamechangers-prod.cloudfunctions.net/wearables-openWearablesWebhook`
       - HMAC algorithm: SHA-256
       - Header name: `X-OW-Signature` (must match what `functions/wearables/src/openWearablesWebhook.ts` reads — verify by grep).
       - Webhook secret: same `HMAC_SECRET` from step 5.

    9. Author `docs/operations/open-wearables-hetzner-deploy.md` with: VPS specs, DNS record details, exact Caddyfile, Docker Compose `.env` template (no secrets), provider portal links, webhook config screenshot path. Append to `docs/wearables/open-wearables-deploy.md` a "Production deploy" section linking to the new ops runbook.
  </action>
  <verify>
    <automated>curl -fsSI https://ow.gamechangers.gg/health 2>/dev/null | grep -E 'HTTP/(1.1|2) 200'</automated>
  </verify>
  <done>
    VPS reachable on stable HTTPS URL; OW 0.4.3 deployed via Docker Compose; provider OAuth apps registered (per operator's Phase 2 priority — at minimum Garmin + Fitbit if Whoop/Polar/Oura defer); outgoing webhook configured to point at Cloud Function URL; runbook captured.
  </done>
</task>

<task type="auto">
  <name>Task 3: Sync OW_HMAC_SECRET + end-to-end smoke test + MANDATORY test-user erasure cleanup</name>
  <files>
    docs/operations/open-wearables-hetzner-deploy.md
  </files>
  <read_first>
    functions/wearables/src/openWearablesWebhook.ts (confirm: `defineSecret('OW_HMAC_SECRET')`, `verifyHmacSha256(req.rawBody, signature, OW_HMAC_SECRET)`, `X-OW-Signature` header name),
    .planning/phases/02-platform-mvp/02-15-PLAN.md (Cloud Tasks erasure path — required for MANDATORY cleanup),
    .planning/phases/02-platform-mvp/02-11-PLAN.md (BigQuery export pipeline — needed to confirm cleanup propagates)
  </read_first>
  <action>
    1. Pre-flight: confirm 02-15 + 02-11 outputs exist.
       ```
       gcloud tasks queues list --location=southamerica-east1 --project=gamechangers-prod --format='value(name)' \
         | grep -E 'erasure-hard-delete|dsar-export' | wc -l
       # MUST output 2. If 0 or 1, halt — Plan 02-15 has not completed; mandatory cleanup (step 4) has no path.

       pnpm exec firebase ext:list --project gamechangers-prod | grep -c bq-export-
       # Should be 8. If 0, Plan 02-11 has not completed; BigQuery purge fallback (step 4) cannot be reasoned about.
       ```

    2. **Replace OW_HMAC_SECRET placeholder**: 260509-nmk set a placeholder in Secret Manager. Replace with the REAL HMAC_SECRET from OW `.env`:
       ```
       echo -n "$REAL_HMAC_SECRET" | gcloud secrets versions add OW_HMAC_SECRET --data-file=- --project=gamechangers-prod
       gcloud secrets versions list OW_HMAC_SECRET --project=gamechangers-prod
       # Disable the old placeholder version explicitly:
       gcloud secrets versions disable <OLD_VERSION> --secret=OW_HMAC_SECRET --project=gamechangers-prod
       ```

    3. Redeploy the wearables codebase so the Function picks up the new secret version: `pnpm exec firebase deploy --only functions:wearables --project=gamechangers-prod`. (Cloud Functions v2 reads secrets at cold-start; redeploy ensures the latest version is bound.)

    4. **End-to-end smoke test**:
       - Pre-req: a test user exists in production with `wearable_data` consent granted (or create one). Note the test uid.
       - Pre-req: a `/users/_lookup/wearable/<provider>/<providerId>` mapping exists pointing at the test uid (this is what `connectDevice` writes — operator can write directly via Firestore console for the smoke test if no real OAuth flow is feasible).
       - Trigger a sample delivery: from OW admin UI or via `POST /webhooks/test` (if 0.4.3 exposes one), force-send a test sample for the test uid. Alternative: log into the test wearable account (e.g., a personal Garmin), let OW pull a sample naturally, and wait for the outgoing webhook fire.
       - Verify Firestore: `gcloud firestore documents get users/<testUid>/healthSamples/<yyyy-mm>/metrics/<sample-id> --project=gamechangers-prod` (or via Firebase console). The doc must exist with `source: 'open-wearables'`.
       - Wait 60s + verify daily rollup: `users/<testUid>/healthDaily/<yyyy-mm-dd>` exists with computed values.
       - Cloud Function logs: `gcloud functions logs read wearables-openWearablesWebhook --project=gamechangers-prod --region=southamerica-east1 --limit=20` — must show `verifyHmacSha256` succeeded and the consent gate passed.

    5. **MANDATORY test-user cleanup (Major-6 fix — was previously "optional")**: production health data on a test user lingering past the smoke test violates LOPDP active-deletion duty. This step is no longer optional.

       a. **Confirm BigQuery exposure**: run `pnpm exec firebase ext:list --project gamechangers-prod | grep -i healthSamples` — output MUST be empty (ADR-009 invariant from 02-11). Confirms the smoke-test sample did NOT mirror to `gw_analytics`.

       b. **Confirm `healthDaily` exposure**: `pnpm exec firebase ext:list --project gamechangers-prod | grep healthDaily` — output WILL show the bq-export-healthDaily extension. The smoke-test daily rollup HAS mirrored to `gw_analytics.healthDaily_changelog`. This is on the BigQuery 30d auto-purge schedule when the Firestore source is deleted (assuming the extension's BACKFILL/TIME-TO-LIVE is configured; if not, document the manual purge below).

       c. **Trigger account erasure via Cloud Tasks**: invoke the production `accountErasure` callable for the test uid (per Plan 02-15's Cloud Tasks-backed path):
          ```
          # From an authenticated context (operator's Firebase admin shell or a one-shot script):
          curl -X POST https://southamerica-east1-gamechangers-prod.cloudfunctions.net/consent-accountErasure \
            -H "Authorization: Bearer $(gcloud auth print-identity-token)" \
            -H "Content-Type: application/json" \
            -d '{"data":{"uid":"<testUid>","confirmation":"DELETE"}}'
          ```
          This kicks off the soft-delete + 72h Cloud Task scheduled hard-delete (per Plan 02-15 contract).

       d. **Verify the Cloud Task was enqueued**:
          ```
          gcloud tasks list --queue=erasure-hard-delete --location=southamerica-east1 --project=gamechangers-prod --format=json \
            | jq '.[] | select(.httpRequest.body | @base64d | contains("<testUid>"))'
          ```
          Must return one task with scheduleTime ~72h in the future. Document task name + scheduleTime in runbook Appendix C.

       e. **BigQuery `_changelog` purge timeline**: per Plan 02-11's firestore-bigquery-export configuration, deletion of source Firestore docs creates a DELETE row in `_changelog` (not a true purge). The DELETE row is excluded from `gw_b2b_views` by `WHERE _change_type != 'DELETE'`. For full physical removal:
          - **30-day default**: extension's TTL settings (if enabled in Plan 02-11) physically purge DELETE rows after 30d.
          - **Immediate-purge fallback**: if TTL is not configured or operator wants faster removal, run a one-time DML against `gamechangers-prod.gw_analytics.healthDaily_changelog` deleting rows where `document_id` matches the test uid. Document the SQL in runbook Appendix C.
          - **DSAR fallback for full audit**: per CNST-08 / Plan 02-15, DSAR export of the test uid should produce zero downstream records after the 72h hard-delete completes — this is the strongest external proof of cleanup. Run a DSAR against the test uid 73h after step (c) and confirm empty payload (or near-empty: only audit log entries with the erasure event).

       f. **Document the cleanup chain** in runbook Appendix C: timestamp of accountErasure call, Cloud Task ID + scheduleTime, BigQuery purge approach taken (TTL or DML), DSAR confirmation timing.

    6. Append the smoke-test trace (sample id, timestamps, document paths) AND the cleanup chain to runbook Appendix C.

    7. **HMAC rotation cadence**: document in runbook a quarterly rotation procedure: generate new secret -> update OW `.env` -> restart docker compose -> write new Secret Manager version -> redeploy wearables function -> disable old version. Add a calendar reminder reference.
  </action>
  <verify>
    <automated>gcloud secrets versions list OW_HMAC_SECRET --filter=state=ENABLED --project=gamechangers-prod --format='value(name)' | wc -l | grep -F '1' &amp;&amp; gcloud tasks queues list --location=southamerica-east1 --project=gamechangers-prod --format='value(name)' | grep -E 'erasure-hard-delete|dsar-export' | wc -l | grep -F '2'</automated>
  </verify>
  <done>
    Real HMAC secret synced; placeholder version disabled; wearables function redeployed; one real sample observed end-to-end; test user account erasure has been triggered via Cloud Tasks (Major-6 fix — mandatory, not optional); BigQuery purge approach documented (TTL or manual DML); runbook captures the smoke-test evidence + cleanup chain; rotation cadence documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wearable provider -> OW hub | Provider OAuth tokens; OW manages; refresh tokens stored in OW PostgreSQL |
| OW hub -> Cloud Function | HMAC-SHA256 over raw body; X-OW-Signature header |
| Hetzner VPS -> public Internet | UFW restricts to 22/80/443; SSH key only |
| Caddy -> Let's Encrypt | Auto-cert renewal; ACME HTTP-01 challenge |
| Test user health data -> production storage | LOPDP active-deletion duty after smoke test; mandatory accountErasure (not optional) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16-01 | Spoofing | Forged webhook from non-OW source | HIGH | mitigate | Plan 09 already implements verifyHmacSha256 timing-safe; this plan ensures the secrets MATCH on both sides |
| T-02-16-02 | Information Disclosure | Provider refresh tokens stored unencrypted in OW Postgres | MEDIUM | accept | OW 0.4.3 stores refresh tokens in DB; Hetzner full-disk encryption + UFW + SSH-key-only access mitigate; Phase 3 evaluate Cloud KMS encryption-at-rest fork |
| T-02-16-03 | Tampering | OW upstream releases break our webhook contract | MEDIUM | mitigate | ADR-011 fork strategy: pin to v0.4.3 commit; subscribe to upstream releases; review before bumping |
| T-02-16-04 | DoS | OW hub unreachable -> no wearable data flow | LOW | accept | Manual fallback (self-reports + phone pedometer) is the design floor per Pitfall #9 + Plan 08; uptime SLA not committed |
| T-02-16-05 | Information Disclosure | OW admin panel exposed publicly | HIGH | mitigate | Document in runbook: restrict admin endpoints via Caddy basic auth or VPN; do NOT expose `/admin` over public Internet |
| T-02-16-06 | Tampering | HMAC secret rotation drift between OW and Secret Manager | MEDIUM | mitigate | Task 3 step 7: documented rotation procedure; quarterly cadence |
| T-02-16-07 | Compliance | Test user health data lingers in production -> LOPDP active-deletion violation | HIGH | mitigate | Task 3 step 5 (Major-6 fix): mandatory accountErasure via Cloud Tasks; BigQuery purge timeline documented; DSAR fallback for verification |
</threat_model>

<verification>
- `curl -fsSI https://ow.gamechangers.gg/health` returns 200 with valid TLS.
- `gcloud secrets versions list OW_HMAC_SECRET --filter=state=ENABLED --project=gamechangers-prod` returns exactly 1 enabled version.
- A real sample exists at `/users/<testUid>/healthSamples/<yyyy-mm>/metrics/<id>` with `source: 'open-wearables'` (smoke-test evidence captured in runbook).
- Daily rollup at `/users/<testUid>/healthDaily/<yyyy-mm-dd>` exists with computed values.
- Cloud Function logs show successful HMAC verify + consent gate pass.
- **Mandatory cleanup**: a Cloud Task targeting `erasureHardDelete` with the test uid in body is observed in the `erasure-hard-delete` queue with scheduleTime ~72h ahead (Major-6).
- BigQuery `_changelog` purge approach (TTL or manual DML) is documented in runbook.
- Runbook documents quarterly HMAC rotation procedure.

**OR (deferred path)**:
- If Task 1 decision was `defer`: plan SUMMARY records `BLOCKED-DEFERRED` with the trigger to re-run (VPS budget approved); other Phase 2 functionality unaffected.
</verification>

<success_criteria>
- G8 closed (or explicitly deferred with documented re-trigger).
- T3 acceptance test from 02-VERIFICATION.md is satisfied (or remains `blocked` with documented reason).
- LOPDP active-deletion duty satisfied for the smoke-test data via Cloud Tasks erasure (Major-6 fix).
</success_criteria>

<output>
After completion, create `.planning/phases/02-platform-mvp/02-16-SUMMARY.md` documenting:
- Decision branch taken (proceed vs defer)
- If proceed: VPS public IP, OW URL, providers registered, smoke-test sample id + timestamp, cleanup chain (accountErasure call timestamp + Cloud Task ID + scheduleTime + BigQuery purge approach + DSAR confirmation timing)
- If defer: explicit `BLOCKED-DEFERRED` note + re-trigger conditions
- HMAC rotation cadence
- Runbook reference
</output>
</content>
</invoke>